# Testing Confidence

This document is a current-state confidence index for local testing lanes.
It records the best current evidence only, not a historical run log.

Maintenance rule:

- keep current status, freshness, minimum evidence, durable artifact pointers,
  and known gaps
- do not accumulate rerun chronology, long artifact lists, or investigation
  history here
- put detailed run history, failures, and sequence notes in task notes,
  tracker entries, or saved artifacts, then summarize only the durable outcome
  here
- if the best evidence changes, update the lane row instead of appending more
  narrative

Lane status shorthand:

- Healthy: fresh evidence exists and matches the current environment
- Unknown: the lane exists, but there is no current proof in this workspace
- Blocked: the lane cannot be treated as proof on this machine
- Flaky: the lane is unreliable enough that it should not stand alone

Freshness rule:

- lane statuses are point-in-time evidence, not evergreen certification
- refresh a lane after toolchain, image, browser, Compose, repo, or runtime
  changes that can affect it
- if evidence is stale for the decision at hand, downgrade the lane until it
  is rerun

## Current Status Index

| Lane | Status | Best current evidence | Freshness / notes |
| --- | --- | --- | --- |
| Python web/auth backend | Healthy | Final focused verifier coverage passed the 79-test WebApp slice, 106 combined WebApp/admin/recovery tests, 87 config tests, 35 history/notification/stream tests, 9 host-validation tests, and 26 MigrationWebApp origin/CSRF tests. Fresh Docker image `sha256:19c05aca940012d33c433556dd5afc07d098ec7f25654fc8cbbc51f0d7c8e9bb` reached Dashboard, Settings, Logs, recovery, status, admin APIs, and streams without credentials in explicit open mode; invalid credentials did not narrow access, a cookie-free API-key lifecycle returned 201/200/200/204, hostile cross-origin mutations returned 403, and restarting the same config with env `0` restored `/bootstrap` plus 401 API protection. The prior 1701-test WSL sweep remains the broad backend baseline. | Fresh as of 2026-08-08. Durable evidence: `tmp/pytest/browser-auth-disable-full-open-verifier/`. |
| Concurrent SSE/browser responsiveness | Healthy | Final WSL web/server verifier passed 94 tests. Rebuilt Docker image `sha256:ef1085979c962f6396b865182e35cfcbd05241c271d2b636edf7eb0ed87c0542` admitted 12 authenticated SSE streams with initial events, rejected the 13th promptly with 503, kept normal and invalid-auth requests responsive, recovered capacity after disconnect and restart, and remained responsive with empty/partial clients. Three simultaneous independent Playwright contexts completed the real API-key remember flow, reached the dashboard, and established HTTP 200 streams with initial status events. | Fresh as of 2026-08-03. Detailed evidence: `tmp/pytest/api-key-local-dns-responsiveness-verifier/`; graceful quiet-client release was bounded at about 16 seconds in the saturated probe. |
| Scoped model streaming and container memory | Healthy; bounded pair-scoped streaming verifier-closed | Lossless V2 manifest/root/completion frames are capped at 64 KiB per line, enforce dialect/order/EOF authority, and reconcile absence only for the healthy owning pair. Final WSL scanner/model/controller slices are green. Exact Docker image `sha256:f4963a9a0c16ab411d0ed303e4dc142a2d345c4e883f39621218dd5a9d49b876` reconciled six pairs, including 161 roots plus an 800-child tree; scoped SSE returned total `161` with a 100-record page, no pair showed a false `Deleted` row, and restart recovered every pair. Prior WAN/outage and memory evidence remains current. | Fresh as of 2026-08-11. Stable post-restart Playwright had no browser or HTTP errors. Callback-mode V2 remains slightly more tolerant of bounded non-protocol trailing lines than one-shot mode; this cannot mutate parsed records and is a low-priority strictness follow-up. |
| Historical logs and AI diagnostics | Healthy | Final verifier: 48 passed with 2 platform skips; Angular log service and page specs passed 8/8 each. The admin-only `seedsync.log-history.v1` API, bounded scan/record/response output, canonical/path-aware redaction, private JSONL storage, rollover/restart behavior, and Docker/API storage flow passed; Playwright confirmed compact controls, ERROR filtering, and the accessible failure state. | Fresh as of 2026-07-12; maintainer-approved UI and independent reviewer/security gates passed. |
| Python unit/backend | Healthy | Current focused backend evidence covers controller/model-update, bounded scanner framing, pair-aware absence reconciliation, split-root publication/retry gating, SSH child cleanup, WSGI timeouts, auto-queue, extraction, and remote-scanner redaction. The current critical WSL overlay passed 191 ModelBuilder, 44 ModelUpdater, and 361 Controller tests with no failures or skips; the broader live 1701-test WSL baseline remains current. | Fresh as of 2026-08-11. Exact regression coverage now includes retained-progress cache reuse, 100 ms active versus 1 s idle LFTP polling, status-loss completion, asynchronous equivalent-content proof, metadata-equal/content-different conflicts, durable claim recovery, restart/nonpending and stopped collisions, staging-only extras, final-only extras, and clean-coverage retry resumption. |
| Global Strict Pyright | Gap | The earlier fifteen scoped strict configurations were green, but the current repository-wide Pyright 1.1.410 invocation reports 478 diagnostics. The progress-trace changed-file rerun reports 32 existing controller/model-updater diagnostics after its candidate-specific errors were removed. | Fresh as of 2026-08-08. Runtime and focused test evidence remains healthy, but strict typing must not be claimed green until the current configuration/baseline is reconciled. |
| Queue authority and stop/resume | Healthy | Final revised-candidate WSL coverage passed 158 ModelBuilder, 243 Controller, 68 common/config, 10 stream-handler, and 10 breadcrumb-integration tests; the exact duplicate-basename trace case passed 5/5. Angular passed 25/25 focused browser-service specs and the production build. Clean isolated Docker/Playwright validation on image `sha256:d57e62c8043d0c651effbfb7aa486477270e6bd6236cfd84e90661aa4abd089d` automatically captured an unselected 2 GiB transfer with nine matching arbitration, no-rebuild, stream-enqueue, and stream-emit records, browser receive/apply logs with increasing bytes, no page errors, bounded 128-entry retention, and a hot-disable drain/reset to zero. Prior live 4/4/16 topology evidence also proved two-second Stop readiness, parser-valid LFTP checkpoints, coalesced Queue dispatch, and exact path-pair-aware STOP routing. | Fresh as of 2026-08-09. Durable evidence: `tmp/pytest/progress-pause-instrumentation/verifier-global/`, `tmp/pytest/stop-resume-flicker/verifier/`, and `tmp/pytest/stop-checkpoint-verifier/`; strict Pyright remains a repository-baseline gap rather than candidate-green evidence. |
| Migrated file status reconciliation | Healthy | WSL verifier coverage passed the model-builder, updater, serializer, AutoQueue, and controller reconciliation slices, including the exact 15-test persistence/restart/pruning rerun. Dockerized Angular passed all 111 task-owned specs. A rebuilt candidate image served `/bootstrap` with the workspace serializer, and Playwright proved Local Only, Deleted, empty-directory, and zero-byte behavior across four rows, three isolated same-basename path-pair identities, and a scoped `model-updated` transition with no browser errors. | Fresh as of 2026-08-03. Durable browser evidence is in `tmp/pytest/verifier-status-reconciliation/playwright-matrix.json`; the broad Angular run's unrelated modal fixture failure remains the bounded lane gap below. |
| Outbound notifications | Healthy | Provider-aware auth/redaction, SSRF-safe pinned transport, bounded queue/retry behavior, download/extraction/successful-remote-delete completion events, and default-off exactly-once `download_start` with authoritative stale-state pruning are covered for Generic Webhook and Apprise API. | Fresh as of 2026-07-11: focused backend/controller and Angular passes plus independent reviewer/security passes are green; exact live `lscr.io/linuxserver/apprise-api` and signed Generic Webhook delivery were confirmed, and Docker/Playwright passed on full image `sha256:1766e2412ee052d49f837f8868df1cd72b654b85f71e2ee3b59b2e20a199f1bf`. |
| Python integration/controller | Healthy | The final WSL verifier at `tmp/pytest/runs/20260720-113859` completed 57 nodeid batches across 91 files with 1701 selected/executed (1700 passed, 0 failed, 1 intentional stress skip, 0 harness errors), including live SSH/LFTP/controller and archive-backed coverage. | Fresh as of 2026-07-20; Docker remote `127.0.0.1:1234` and app HTTP `127.0.0.1:8800` were healthy. |
| Python spawned process/runtime | Healthy | Guarded `spawn`, queue-based child logging, process lifecycle cleanup, and real scanner/extract/validate/delete startup remain covered. Remote scanning now uses a controller-owned spawn-safe advisory lease shared across refreshed generations, while each six-pair scan runs serially. A real Linux spawn probe held cross-generation children apart for about 3.001 seconds; exact Docker logs showed each pair command starting only after the preceding return code, including after restart. | Fresh as of 2026-08-11. The conservative global serialization trades cross-host throughput for shared-host compatibility; unsupported advisory-lock filesystems remain a documented non-blocking portability risk. |
| LFTP credential process isolation | Healthy | Final WSL verifier passed 174 tests with 34 environment skips. Real SFTP password and key authentication and real verified-CA FTPS authentication, listing, and protected data transfer passed with the password absent from child argv/environment and retained artifacts; FTPS wrong-password and untrusted-certificate probes failed closed with sanitized output and clean child exit. Fresh image `sha256:99adfb3aa15df2854e940141dc8aeae3f0413fd4203139edf39e2317bdcd6d83` passed first claim, authenticated config/status/stream checks, Settings inspection, and remembered-browser restart in Playwright with no browser errors. | Fresh as of 2026-08-03. The config-file-only legacy argv rollback is omitted from serialized config/UI, blocked through the API, and documented as process-visible; detailed artifacts are under `tmp/pytest/lftp-credential-process-argv-verifier/`. |
| Final-move failure/retry and portable publication | Corrective image published; live reconciliation pending | Capability-aware no-clobber publication remains green for files, directories, symlinks, FUSE fallback, metadata, and collision residue. Equivalent final/staging leaves are privately claimed and proven by one 16 GiB-capped asynchronous exact-byte comparator; durable owner-scoped sidecars recover claims after cancellation, refresh, reset, or crash. Manual terminal-collision retry now requires fresh authoritative state before that byte proof can settle a duplicate. Late legacy retirement cleanup removes only bounded, exact-name, same-device empty directory trees and rejects files, symlinks, reparse points, mounts, bind mounts, cross-device entries, overflow, and races. | Fresh as of 2026-08-11. The corrective WSL slices passed 64 Controller, 21 ModelBuilder, 75 diagnostics/core, and 20 scanner tests; the broader Controller/ModelBuilder/ModelUpdater run passed 616. Independent correctness and data-loss reviews passed. Published index `sha256:cc929142ac5969799381cdb49b4f035ade4a9827da31268f15b5323ec6fb9724` passed exact-digest HTTP and Playwright smoke; live House and Maid settlement remain the Unraid acceptance gate. |
| WSL backend stop-state integration | Healthy | Targeted WSL/Linux stop-state slice for the backend contract. | Fresh as of 2026-03-30. |
| Host Angular/Karma full-suite proof | Healthy for host comparison only | Node 24 host Angular/Karma suite completes successfully. | Comparison evidence only; not the supported closure lane. Fresh as of 2026-04-01. |
| Dockerized Angular build/test | Healthy with one bounded unrelated failure | The supported Linux Chrome 151 lane previously passed 493/493, including the large-model regression that forbids eager full Immutable collection materialization during view rebuilds. The current status-presentation slice passes 76/76 and its production build plus Docker-served Playwright acceptance path are green. Two current full runs each passed 501/502; only the unrelated modal overlay geometry fixture failed because its queried confirm element was absent. | Fresh as of 2026-08-08. Current browser evidence is `tmp/pytest/status-visible-playwright.json`; the modal fixture is tracked as a bounded lane gap below. |
| Angular files-page large-list virtualization and initial load | Healthy | Focused virtualization coverage remains current. Fresh image `sha256:2af945945b9e26d29f5b0e6c1179551b866ce7d3efcab8736146da3225cf58b2` served Docker-authenticated Playwright at 543 roots/1,552 nodes and 2,000 roots/8,000 nodes with 50 mounted rows, no browser errors, no full-model debug payload, and model-dispatch-to-useful times of 389.1 ms and 484.7 ms. Sort/filter stayed within 150 ms at both scales. A later rebuilt pagination candidate passed 79 focused specs and Docker/Playwright with exact `25/50/100/500/1000/All` choices, clean-origin All default, persisted 500/50/All reloads, 1,800-file ingest, 16 mounted rows, a roughly 420 ms switch to 500, and no stable browser errors. | Reviewer and verifier passed. Fresh as of 2026-08-03. Durable evidence: `tmp/pytest/large-model-initial-load-latency/verifier/browser-candidate/browser-baseline-summary.json`; the full pagination persistence matrix is `tmp/pytest/tmp/pytest/files-pagination-r3-matrix.json` (captured during restart/SSE reconnect), and the clean stable timing/error check is `tmp/pytest/tmp/pytest/files-pagination-r3-stable.json`; prior interaction/scroll evidence remains at `tmp/virtualized-files-final-verifier/final/live-clone/summary.json`. |
| Angular mobile file-row progress layout | Healthy | Final verifier passed 68/68 focused Angular tests, production build, rebuilt Docker/current assets, and authenticated Playwright with 1,220 files at 360/390/760/899/900/1440px. Details, long names, ARIA semantics, selection/bulk actions, virtualization remeasurement, and no-overflow/error checks passed. | Maintainer-approved; reviewer reported no findings; desktop layout preserved. Fresh as of 2026-07-26. Durable evidence: `tmp/mobile-progress-final-verifier/final/proven-helper/summary.json`. |
| v0.8.6 historical upgrade and recovery | Corrective image published; own-install retest pending | The retained `ship-20260801-r54` transfer, extraction, restore, reboot-parity, and audit evidence remain current. The live Unraid rollout exposed two bounded post-migration handoff failures in the earlier image: an indefinitely pending proxy-held `/bootstrap` readiness request, followed by false completed-lineage failure after the ephemeral first-claim proof expired. The durable live receipt and retained backup prove the migration itself completed. The correction bounds and retries readiness requests, accepts only the exact empty expired-proof lifecycle (while rejecting consumed or incoherent histories), reads bounded old-image histories up to 1 MiB, and coalesces unchanged future snapshots. Focused Angular tests passed 25/25; focused WSL auth/lineage tests are green; and the rebuilt full image restarted the retained completed/unclaimed lab with a 205 KiB history, redirected `/dashboard` to `/bootstrap`, rendered `SeedSync browser access` in Playwright without browser errors, kept `/server/status` at 401, and stopped duplicate 30-second history growth. A fresh candidate image also served an isolated v0.8.6 migration-required runtime where `/dashboard` and `/dashboard/` redirected to `/migration`; Playwright reached the visible `Migration required` checkpoint for both paths. | Fresh as of 2026-08-03. First-run migration and administrator claim remain intentionally open to any reachable client until the first successful claim; unfinished setup must stay on a trusted network. The corrected image is published, and the maintainer-owned movie retest remains the rollout gate. |
| Docker release publication | Current critical-fix image published and registry-checked | `truedarkdev/seedsync:0.9.1` and `truedarkdev/seedsync:latest` resolve to OCI index `sha256:cc929142ac5969799381cdb49b4f035ade4a9827da31268f15b5323ec6fb9724` for `linux/amd64`, `linux/arm64`, and `linux/arm/v7`, with three provenance attestations. The corrective source commits are `cd3935266ced876481ddea9e5b91a7d57082a664` and `364ad49b28c8437156fb8df927431425861f8104`; the patch version intentionally remains `0.9.1`. The exact immutable staging digest returned `/bootstrap` 200 and unauthenticated `/server/status` 401, and Docker-served Playwright rendered `SeedSync browser access` without page or console errors. | Fresh as of 2026-08-11. Both public tags were promoted from the tested staging digest without rebuilding and resolve back to the same digest. No GitHub push or tag was performed. The running Unraid container was still on prior index `sha256:e045fa83f1286e04f8b87f97421cf0cfebe8a1bfbe5890d526181ed26c934a3d` at the post-publication check, so deployment remains the rollout gate. |
| Dependency tooling / Dependabot | Healthy with bounded gaps | Dependabot v2 structural/exact validation passed; Poetry check passed; healthy Docker runtime plus authenticated Playwright dashboard/settings evidence remain current. GitHub server-side validation is unavailable; npm coverage intentionally targets direct `/src/angular/package.json` only because the generated lockfile is untracked. | Fresh as of 2026-07-12; GitHub Actions automation and Angular transitive lockfile/`npm ci` hardening remain deferred. |
| Angular 21 standalone migration | Healthy | Final verifier confirmed the remapped standalone architecture in a fresh exact Docker image: configured remote scan, two-browser remembered-auth flow, dashboard/settings/logs/about routes, `/server/status` 200, and `/server/stream` 200; production has `bootstrapApplication`/standalone components with no `AppModule`/`NgModule`. | Maintainer-approved visual behavior; reviewer/security passes completed. Fresh as of 2026-07-10. |
| Docker runtime/process supervision | Healthy | The config-root contract remains current in strict and legacy non-root modes. Published index `sha256:036c43730356ff19163bde3c9c88ec32eb953fe8521d1e375316551a36dff7d9` passed UID/GID `99:100` with an anonymous, unused `/downloads` volume and an active arbitrary root at `/data/root-a`; the same implementation passed the original explicit writable `/downloads` deployment. Active unwritable `/data/root-c` failed the controller closed while keeping the browser recovery surface available with the exact remediation error. Earlier unknown-identity, `1101:1102`, idempotence, and retained-backup evidence remains current. | Fresh as of 2026-08-10. Stable authenticated Playwright sessions passed before and after restart with zero browser errors; durable evidence is under `tmp/pytest/path-pair-deployment-continuity-completion/` and `tmp/pytest/docker-publish-6c1223415/`. |
| Docker runtime performance | Settled-idle target met locally; periodic-refresh and active-responsiveness gates failed | The exact six-pair, 384,000-file local Docker profile now mirrors the live topology and keeps breadcrumbs enabled. Candidate `sha256:8513f0fdd414c18a4dd0c0ffb288457fdceb247016a2799ca2571f267dfdd6e2` reached 214,974 merged nodes and produced 11 settled samples at 8.50% average and 9.83% peak CPU after bounding unchanged model and breadcrumb work. The same 150-second window crossed the configured 120-second remote refresh and exposed a remaining 131.41% peak / 28.47% average, attributed to remote transport, stream parsing/publication, and builder synchronization. Exact-image Playwright measured 34 distinct progress mutations over 15 seconds at 415.9 ms p50 / 628.9 ms p95 and a repeat reached 1,476 ms p95; queue took 610.8 ms while Stop still succeeded without browser errors. | Fresh as of 2026-08-11. Both the formal CPU and <=200 ms active-progress gates fail, so the candidate is not publishable and the existing 0.9.1 Docker Hub tags were not changed. Evidence: `tmp/pytest/performance-lab/candidate-v091-deadline-final-20260811/candidate/metrics-summary.json` and `tmp/pytest/e2e-v091-deadline-cadence-final.png`. The task remains open until unchanged periodic refresh and synchronous LFTP-status/controller-thread blocking are corrected without weakening final-move correctness. |
| Angular sidebar path-pair refresh UI | Healthy | Targeted Angular spec plus live Docker/Playwright proof for immediate path-pair visibility on load and reload. | Fresh as of 2026-04-04. |
| Angular dashboard path-pair startup UI | Healthy | Targeted Angular spec plus live Docker/Playwright proof for correct dashboard startup routing and render, direct path-pair cards, responsive 3/2/1 layout, and ETA visibility on active cards only. | Fresh as of 2026-04-04. |
| Angular sidebar version readout | Healthy | The repo-supported Docker Angular lane passed the focused AppComponent spec 9/9 in Chrome Headless 151. Docker-served Playwright previously proved the package-derived footer layout and navigation behavior; the rebuilt 0.9.1 release candidate rendered `v0.9.1` with no browser or HTTP errors. | Maintainer visually approved the version-first design. Fresh as of 2026-08-10. Durable layout evidence: `tmp/ui-version-toolbar-verifier/`; release check: `tmp/playwright/release-v091/`. |
| Path-pair settings and AutoQueue overrides | Healthy | `cae3ad3e4` covers disabled legacy Server/Local Directory and global AutoQueue controls when pairs are enabled, zero-enabled restoration, shared-pattern behavior, and stale-buffer transitions; `91f921407` covers startup legacy-path fallback. Focused Angular/Python tests plus Docker/Playwright checks passed; authenticated Playwright Settings confirms the Remote Python Path default copy and inline-validation/xfer_verify wording. | Fresh as of 2026-07-12. |
| Path-pair deployment continuity | Healthy and published | The retained-state relocation/adoption baseline remains green, and the deployment startup gap is covered by 48 focused WSL verifier tests plus fresh Docker/Playwright matrices. UID/GID `99:100` starts with no explicit `/downloads` bind when the enabled pair uses `/data/root-a`; an unmounted disabled `/data/inactive-root` stays optional. A fresh container adopted `existing-a.bin` without queueing or rewriting it, downloaded only the remote-only fixture, retained the exact pair mapping, and survived replacement and restart. The original `/downloads` topology likewise adopted its existing file unchanged and survived restart. An active unwritable root fails clearly without activating the controller. | Fresh as of 2026-08-10. Published image `sha256:036c43730356ff19163bde3c9c88ec32eb953fe8521d1e375316551a36dff7d9`; durable evidence: `tmp/pytest/path-pair-deployment-continuity-completion/` and `tmp/pytest/docker-publish-6c1223415/`. The first-claim transition emitted the known one-time SSE console error; every stable remembered-browser run was clean. |
| Path-pair marker canonicalization | Healthy | Final WSL verifier slices passed 537 tests with 5 environment skips; the Dockerized Python slice passed 47 tests with 1 Docker-specific warning assertion deselected. A rebuilt candidate converted all five identity-bearing persistence collections from v0.8.6/legacy inputs to three exact canonical path-pair IDs, resolved count collisions by maximum value, and remained semantically identical after restart. Live Docker/Playwright used the real path-pairs API and remembered auth to prove three same-basename rows stayed isolated through a scoped update with no browser errors. | Fresh as of 2026-08-03. The separately gated multi-path integration probe had 3 environment-prerequisite skips; direct persistence and live browser matrices cover the task acceptance path. |
| Downloaded-time and visible file-status sorting | Healthy | The established 519-test WSL and 81-spec Dockerized Angular coverage remains current. The 2026-08-08 start-time correction passed 15 focused WSL lifecycle/timestamp tests, and the Smart Status comparator passed 19/19 focused Dockerized Angular specs across active, actionable, terminal, null, and canonical-tie cases. Live Docker/Playwright proved Downloaded Newest moved a real active transfer from index 16 to 0; Smart Status kept a later active transfer at index 0 and the stopped row at index 1, then ordered completed rows by download recency across the remaining list with no browser errors. A later 76-spec focused run and fresh Docker-served Playwright matrix proved row labels/icons, Status, Status Reverse, status counts, and Downloaded filtering consistently treat hidden downloaded/move-failed lineage as Local Only while retaining raw status for actions and statistics. | Fresh as of 2026-08-08. Visible-status candidate image `sha256:c060f175d258b30af121bb5d23bd4b0a6749aff6f3837d59cfb5ad8137eb6606`; browser evidence is in `tmp/pytest/status-visible-playwright.json`, `tmp/pytest/download-start-time/playwright-live-start.json`, and `playwright-smart-status-live.json`. |
| Path-pair persistence integrity | Healthy with a bounded durability gap | Final reviewed/verifier evidence covers coordinated `ControllerPersist` snapshot/mutation transactions, including AutoQueue marker clearing; 230 controller tests plus exact concurrency/AutoQueue/refresh regressions pass, and the Docker app was rebuilt and remained healthy through the persistence interval with authenticated Playwright bootstrap/Settings/VerifyPair evidence. | Ordinary writes use same-directory atomic replacement. Crash-level rename durability lacks parent-directory fsync, and replacement inode metadata/ACL preservation is not explicitly defined. Fresh as of 2026-07-19. |
| Angular API access UI/service lanes | Healthy | Targeted Angular specs plus live Docker/Playwright proof for API Access create, edit, rotate, revoke, legacy disable/clear, and revoked-key cleanup flows. | Fresh as of 2026-04-04. |
| Legacy Protractor/e2e | Blocked | Not exercised: the Makefile e2e lane requires `SEEDSYNC_DEB` or `STAGING_VERSION`, and the required credentials/tokens are absent. | Exclude this lane from repository-wide green claims; this is an execution blocker, not a test failure. Fresh as of 2026-07-20. |
| Native Windows Poetry path | Blocked | Local Windows Python is outside the repo-supported Poetry range. | Do not treat this host path as proof here. |

## Minimum Evidence Ladder

Use the smallest lane set that honestly proves the change, then widen only
when the change crosses layers or runtime surfaces.

| Change class | Minimum evidence | Notes |
| --- | --- | --- |
| Backend-only | One targeted unit test or narrow controller/integration slice | Prefer controller integration when the change touches controller state, filesystem state, or path-pair handling. |
| Frontend-only | Targeted frontend spec/component coverage | Add browser smoke if the behavior is only observable in the DOM/runtime. |
| Runtime-visible UI behavior | Host Angular/Karma smoke plus browser or Docker/live proof | Host smoke is supportive evidence only. |
| Mixed API/UI | Backend proof plus frontend/browser proof | Add browser/runtime evidence when the live app shell decides the outcome. |
| Transfer / stop-resume / scan / path-pair regression | Targeted backend proof plus WSL/Linux controller integration | Require live Docker validation before closing if the change is user-visible there. |

The ladder is a floor, not a ceiling.

## Freshness And Lifecycle

- treat lane health as working status, not permanent certification
- if a lane fails, flakes, or is blocked by environment or tooling drift,
  downgrade it immediately and note the blocker elsewhere if detail is needed
- if a task depends on runtime-visible proof and the live lane has not been
  exercised in the current environment, do not treat host smoke as a
  substitute
- when a lane is no longer the best proof path for a task, keep it listed but
  stop relying on it as closure evidence

## Gap Escalation

- if the minimum lane set does not prove the task, record the gap here or in
  the tracker instead of leaving it in memory
- if the same gap keeps showing up across tasks, convert it into follow-up
  work
- if live Docker or browser proof is required but blocked, name the blocker and
  treat it as lane confidence debt, not unfinished product implementation

## Durable Artifacts

When exact outcomes matter, write the run to `tmp/pytest/` at the repo root
and keep the artifact name descriptive.

- prefer `--junitxml=tmp/pytest/<lane-name>.xml` for pass/fail-sensitive runs
- keep the full log with `tee tmp/pytest/<lane-name>.log` when terminal output
  may truncate useful detail
- put batch-run artifacts under `tmp/pytest/runs/<timestamp>/` when a scripted
  runner already creates that layout
- save the nodeid or lane label in the filename so later evidence can be
  matched back to the exact command quickly

## Known Gaps

- Dockerized Angular full lane: two 2026-08-08 runs each passed 501/502, with
  only `modal.service.spec.ts` failing because the overlay geometry fixture's
  confirm element was absent. The current status-presentation slice passes
  76/76 and its production Docker/Playwright path is green; keep the unrelated
  modal fixture as a separate lane repair rather than weakening UI assertions.
- Legacy Protractor/e2e: blocked/not exercised because the Makefile lane requires
  `SEEDSYNC_DEB` or `STAGING_VERSION` plus credentials/tokens that are absent;
  exclude it from repository-wide green claims.
- Native Windows Poetry validation: blocked on this machine because the local
  Python version is outside the repo-supported range. Use a supported Python
  environment first if Windows host validation becomes necessary.
- CI/Docker/live-suite hardening from the 845-849 row slice has verifier/final
  validation on the Docker-served app and Docker Python harness. The default
  SSH/LFTP lane keeps mock/unit coverage active while live cases stay gated by
  `SEEDSYNC_LIVE_SSH_TESTS`.
- Scanner directory-symlink coverage is Linux/WSL-only on this host because
  Windows lacks the required symlink privilege. Keep the new symlink tests in
  the Linux/WSL verifier path.
- A broader controller/model-builder/scanner-process probe still reproduces
  seven unchanged expectation failures around downloaded/extracted state and
  targeted scan queues. They are not regressions from the rapidcopy/tini
  candidate, but the broad controller lane needs a separate expectation audit
  before it can be treated as wholly green.
- On Windows-backed WSL `/mnt/c` DrvFS, artifact modes may be mount-derived
  rather than POSIX-enforced; use Windows ACLs or a Linux-owned location when
  POSIX privacy guarantees are required.
