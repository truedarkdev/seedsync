# Post-Integration Audit Archive - rapidcopy 005

This archive chunk contains finished audit rows for `rapidcopy`.

Rows in this chunk: `27`

## rapidcopy

Audit base: `origin/master @ ff2a1039935beccbbf7ec76134b41d2e91137742`
Source branch: `rapidcopy/master`
Fork tip at audit start: `c300b72f808772b00cc977ccceaa23f3c373ce33`
Inventory status: `complete`
Audit state: `in progress`
Pass date: `2026-03-11`

Archive chunk: `005`

| Commit | Upstream commit subject | Mapped integration subject | Triage outcome | Confidence | Evidence | Reviewer needed | Coverage | Final disposition | Follow-up / proof |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `2ed36f269e6456476c79a4418442c08619d7dfd2` | fix: resolve Playwright test stability issues with WebSocket-based app | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | tracker match | no | none | intentionally skipped | Commit only touches `src/e2e-playwright/tests/...`; this repo still uses the legacy `src/e2e` Protractor-era suite, and Subject 2 already records the rapidcopy-specific Playwright stack as intentionally deferred rather than a missed conservative integration. |
| `4c1cc1678f1456142a1f75d82dec9a813671dc20` | fix: use /server/config/get for backend availability checks | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | tracker match | no | none | intentionally skipped | This is a follow-up fix inside the deferred `src/e2e-playwright/tests` stack, which does not exist locally; the same Subject 2 Playwright deferral covers the `/server/config/get` health-check adjustment as part of that skipped verification replatform. |
| `5d5a90a099b5ecdafe844f5a1f17c19861ed012c` | fix: add pickle fallback to remote_scanner for legacy scanfs binaries | Subject 15 - Scanning | likely intentional skip | high | tracker match | no | none | intentionally skipped | `doc/integration-tracker.md` records this exact rapidcopy fallback as deliberately not selected because local master already manages a JSON-only `scanfs` producer/consumer contract and avoids reintroducing unsafe pickle deserialization into remote scan transport. |
| `5d3e1e85636def6c5abc91a91a5381adc807b5bf` | fix: add VALIDATING state to model serialization | validation follow-up milestone | covered elsewhere likely | high | tracker match | yes | none | covered elsewhere | The backend hunk depends on the absent validation model/API stack (`VALIDATING` does not exist in current `ModelFile.State`), and the Playwright wait changes sit inside the already-deferred `src/e2e-playwright` suite; reviewer confirmed this should roll into the existing `227b5a34` validation/download-integrity follow-up instead of opening a separate task. |
| `4fdda8c59f8af46bdf09fc287a70922d93ce389e` | docs: add comprehensive testing state documentation | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | tracker match | no | none | intentionally skipped | This docs-only file documents the rapidcopy Playwright/QA migration stack (`docs/TESTING_STATE.md`), which this repo already treats as a deferred verification replatform rather than a conservative import into the current Protractor-era verification path. |
| `4527bfecead67af92e73e83f0b74636256db3e7b` | fix: resolve UI bugs and validation infinite loop | validation/settings follow-up cluster | covered elsewhere likely | medium | behavioral inference | yes | partial | covered elsewhere | Reviewer confirmed this bundled commit does not warrant a new standalone task: the logs `ViewChild` fix is already integrated locally, the validation retry slice belongs under the existing `227b5a34` validation follow-up, the Playwright/about-page pieces are already deferred or reclassified elsewhere, and any remaining checkbox-save issue is better handled under the separate narrower settings row `15a9918`. |
| `aeb27fa17044290df17ad718cbf4781243bf03bf` | fix: use JSON output in scanfs and add missing model states | validation follow-up milestone | covered elsewhere likely | medium | tracker match | yes | partial | covered elsewhere | The JSON `scan_fs.py` transport direction is already present locally through the managed JSON migration, while the added `VALIDATED` / `CORRUPT` serialization states still depend on the separate validation feature chain that remains tracked under the existing `227b5a34` follow-up task. |
| `7663db93ba870176236cfbfbaa0f8668496267a2` | fix: update favicon to match new RapidCopy logo | Subject 21 - Cross-Cutting UX Or Workflow Conflicts | likely intentional skip | high | tracker match | no | none | intentionally skipped | This is another RapidCopy branding-asset swap; the audit already rejects the equivalent favicon rebrand commit `ebe416f8` under the standing rule to preserve SeedSync identity by default. |
| `2614ae61c5924889c77e1cc89abbd1dae0b801f0` | fix: batch remote checksum commands to prevent ARG_MAX overflow | validation follow-up milestone | covered elsewhere likely | medium | tracker match | yes | none | covered elsewhere | The affected `src/python/controller/validate/` checksum path does not exist locally because the broader validation/download-integrity feature chain is still absent; treat this as part of the already-recorded `227b5a34` validation follow-up rather than opening a separate task for a dependent optimization. |
| `1700fcc9aacd6216c07a739b15555ffe7184b113` | test: add comprehensive UI tests for all pages | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | tracker match | no | none | intentionally skipped | This is another `src/e2e-playwright/tests/...` addition inside the deferred Playwright verification stack; local master still uses the legacy `src/e2e` suite and Subject 2 already records these Playwright assets as intentionally deferred rather than missed conservative integrations. |
| `be7c52e01fbf83867dc2508a444f7ac406844a32` | fix: correct Restart button test to check sidebar link | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | tracker match | no | none | intentionally skipped | Narrow test-only fix inside `src/e2e-playwright/tests/settings.spec.ts`; because the repo has not adopted the rapidcopy Playwright stack, this assertion update stays with the standing Playwright deferral. |
| `25145f67dd72514f714366b9b2228d5788d7d786` | config: increase default validation chunk size from 10MB to 50MB | validation follow-up milestone | covered elsewhere likely | medium | tracker match | yes | none | covered elsewhere | The changed `validation_models` path does not exist locally because the broader validation/download-integrity stack is still absent; treat this config-tuning default as part of the existing `227b5a34` validation follow-up rather than a separate audit task. |
| `c392a29a1559c116d7e610767cb98dd11f43cd80` | docs: rewrite README with comprehensive feature documentation | Subject 1 - Documentation And Maintainer Notes | likely intentional skip | high | tracker match | no | none | intentionally skipped | Subject 1 already records this broad README rewrite as too tied to RapidCopy branding, feature-set claims, and modernization messaging that do not match the current branch. |
| `9f4cbb15bac48b66cb0a84cdbc9a9a1439938f49` | docs: add Docker Hub publishing to-do | Subject 1 - Documentation And Maintainer Notes | likely intentional skip | high | tracker match | no | none | intentionally skipped | This one-line README to-do rides on the same skipped rapidcopy README rewrite path and does not warrant a separate docs follow-up in the conservative local documentation set. |
| `d20b84d3b799e0a9a291fadf755dbcd24d43f081` | fix: prevent validation status leak and ensure proper cleanup | validation follow-up milestone | covered elsewhere likely | high | tracker match | yes | none | covered elsewhere | Tracker notes already classify `d20b84d` as part of the deferred validation stack and explicitly say it depends on rapidcopy's validation-process architecture, which is not present in the current base; keep it under the existing `227b5a34` follow-up task instead of opening a new audit task. |
| `4cd7fc1da6a2130047e65109caac271568b2a91c` | fix: preserve validation states across model rebuilds | validation follow-up milestone | covered elsewhere likely | high | tracker match | yes | none | covered elsewhere | Tracker already classifies `4cd7fc1` inside the deferred validation stack; because the branch still lacks the validation model/state architecture, keep this model-rebuild preservation fix under the existing `227b5a34` follow-up task. |
| `b62970aef970c121a2373b9ee74ca9330828f4bc` | fix: show status for local-only files missing remote counterpart | Subject 12 - Files Page And File Operations UI | already integrated likely | high | direct local match | no | full | already integrated | Tracker records local commit `4ae0103` as the adapted equivalent: preserve downloaded-state/file-row hinting for local-only files that lost their remote counterpart, including the `default-local.svg` icon and `Local Only` UI label. |
| `d0662cab2b152f600b2af7b90345197b83eeb9e7` | feat: switch validation from MD5 to xxHash (xxh128) for ~20x speedup | validation follow-up milestone | covered elsewhere likely | high | tracker match | yes | none | covered elsewhere | This checksum-algorithm change is part of the same absent validation/download-integrity stack already opened from `227b5a34`; tracker notes explicitly keep `d0662ca` with that deferred validation surface rather than treating it as a standalone conservative import. |
| `323e3edf31e38e3b7cec2273b54dfae65a029e51` | fix: use ng-bootstrap directives for dropdowns and Bootstrap 5 data attributes | Subject 12 - Files Page And File Operations UI | already integrated likely | high | direct local match | no | full | already integrated | Tracker records local commit `f38666c` as the adapted low-risk dropdown compatibility slice of `323e3ed`, which is why current file options already carry the Bootstrap 5-compatible dropdown attributes. |
| `f1fc34caf11125a2a6f94be5260dade1122f5a89` | feat: add clickable column headers for sorting on the dashboard | Subject 12 - Files Page And File Operations UI | already integrated likely | high | direct local match | no | full | already integrated | Tracker records local commit `f85bad2` as the adapted equivalent that landed the conservative sortable-header slice of `f1fc34c` across the existing files list and sort service. |
| `d9c69750c195915cde988a592b40679effa96f15` | fix: add validation states to status sort priority map | validation follow-up milestone | covered elsewhere likely | high | tracker match | yes | none | covered elsewhere | The added sort priorities depend on `VALIDATING` / `VALIDATED` / `CORRUPT` states that are still absent locally, so this row remains part of the existing validation/download-integrity follow-up rather than a separate standalone gap. |
| `e158fe2a88c57ab29607a34904cfeae1fc0487df` | config: update docker-compose.yml with production volume mounts | packaging/runtime environment docs | likely intentional skip | high | behavioral inference | no | none | intentionally skipped | This compose change hard-codes one contributor's production mount paths and strips repo-generic options; it is environment-specific operational flavor, not a conservative repo-default packaging improvement. |
| `58af9ee80588740b05a766d6ade57d1f06ce7823` | fix: resolve invalid escape sequence warnings in test_job_status_parser | Subject 14 - Transfers And LFTP | possible gap | high | behavioral inference | no | none | needs new integration task | `python3 -m py_compile src/python/tests/unittests/test_lftp/test_job_status_parser.py` still emits multiple invalid-escape `SyntaxWarning`s locally, and Subject 2 notes this parser-test cleanup should be reviewed with the LFTP/parser subject rather than left as an isolated verification import. |
| `bd93c7711ddc4de91216bdbb1146b167ee7aea39` | docs: update AGENTS.md with build environment and pending work | Subject 1 - Documentation And Maintainer Notes | likely intentional skip | high | tracker match | no | none | intentionally skipped | This is a fork-specific `AGENTS.md` working-memory update about the author's build host, SSH access, and session notes; local policy already keeps that kind of transient state out of the durable repo instructions. |
| `394a921c714d77fc9e873ceb177de53bb128f4d4` | config: switch git remote to SSH and update AGENTS.md | Subject 1 - Documentation And Maintainer Notes | likely intentional skip | high | behavioral inference | no | none | intentionally skipped | This is contributor-environment workflow state about one fork's remote URL and local SSH setup, not durable repo policy or portable project configuration. |
| `ab075718e3cb3fb7bca18fd347c2d10f2ae98d97` | chore: update poetry.lock to latest dependency versions | Subject 3 - Dependencies And Build Tooling | covered elsewhere likely | medium | tracker match | no | partial | covered elsewhere | The broad dependency refresh is the kind of large tooling churn this repo has intentionally not imported wholesale; the small `test_job_status_parser.py` hunk is better handled under the separate `58af9ee` follow-up task rather than through this mixed lockfile update. |
| `dda1cb2eda952922887a50932010515832fdfaa9` | feat: implement inline validation (validate_after_chunk) | validation follow-up milestone | covered elsewhere likely | high | tracker match | yes | none | covered elsewhere | Tracker already classifies `dda1cb2` as part of the deferred validation stack spanning backend, settings, UI, and packaging; keep it under the existing `227b5a34` validation/download-integrity follow-up rather than opening a new audit task. |
