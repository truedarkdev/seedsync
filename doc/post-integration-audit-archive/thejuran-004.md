# Post-Integration Audit Archive - thejuran 004

This archive chunk contains finished audit rows for `thejuran`.

Rows in this chunk: `27`

## thejuran

Audit base: `origin/master @ ff2a1039935beccbbf7ec76134b41d2e91137742`
Source branch: `thejuran/master`
Fork tip at audit start: `a8561cdc318460de32de082e3cf33f6b6a0093cb`
Inventory status: `complete`
Audit state: `in progress`
Pass date: `2026-03-11`

Archive chunk: `004`

| Commit | Upstream commit subject | Mapped integration subject | Triage outcome | Confidence | Evidence | Reviewer needed | Coverage | Final disposition | Follow-up / proof |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `7e7289e4ee2ab15364b82c01840e6553987dcc03` | Update compose files to use compose spec format | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `src/docker/stage/deb/compose.yml`, `src/docker/stage/docker-image/compose.yml`, and the test compose files still keep `version: "3.4"`, and the stage/deb compose path has neither the upstream compose-spec cleanup nor any replacement cgroup namespace setting. |
| `0e228818abde8249fe414b1563814e7f8716ce68` | Remove unsupported cgroup attribute from compose file | Subject 4 - Packaging And Install | covered elsewhere likely | high | direct local match | no | full | covered elsewhere | Current `src/docker/stage/deb/compose.yml` already has no `cgroup: host` entry; the service block goes straight from `tty: true` to `tmpfs:`. |
| `b5cf1d2a037634b3a872cfd31834d06dcd1c5266` | Add cgroup: host for cgroups v2 systemd support | Subjects 2, 4, and 5 | likely intentional skip | high | tracker match | no | none | intentionally skipped | Upstream later removed this exact `cgroup: host` workaround in `55e582332eb4ebc31f487d553ad437def762cf30` as invalid, and current local stage/deb compose likewise does not carry it. |
| `87d2d141f9683493406e854bc6739590a5acbaad` | Configure Docker daemon with cgroupns=host for e2e tests | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `.github/workflows/master.yml` still runs deb E2E jobs without the upstream Docker daemon `default-cgroupns-mode: host` setup, while local stage/deb E2E still depends on the unresolved systemd-in-container path. |
| `99e6d9e361bb52092e2462ba2705226101c01bc7` | Fix network-online.target blocking seedsync service start | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `src/docker/stage/deb/ubuntu-systemd/*/Dockerfile` set has no upstream `network-online.target` override, so the systemd-based deb E2E path still lacks this container startup fix. |
| `f65a996907872d846b1da1d0269839f819c94bbb` | Fix stage/deb Dockerfile to match ubuntu-systemd base images | Subjects 4 and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `src/docker/stage/deb/Dockerfile` still lacks `STOPSIGNAL SIGRTMIN+3`, still uses the broken single-string `CMD`, and current `src/docker/stage/deb/entrypoint.sh` still does not match the upstream simplified `exec $@` handoff with fd `3>&1`. |
| `5e2cc8e63bb124041e4199b3f8e5d885eecac705` | Fix multiple e2e test issues found in thorough review | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | partial | needs subject reopen | Current `src/docker/stage/deb/ubuntu-systemd/*/Dockerfile` set still lacks the stronger `network-online.target` override from this follow-up, and current `src/docker/stage/deb/install_seedsync.sh` still omits the upstream `set -e` hardening. |
| `f25353f0dfd1ba8054597cfce221080cb7350692` | Use /sbin/init instead of /lib/systemd/systemd in stage/deb | Subjects 4 and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `src/docker/stage/deb/Dockerfile` still hardcodes `/lib/systemd/systemd` in `CMD` instead of the upstream cross-Ubuntu `/sbin/init` path. |
| `f4b496b8f02a61dda01e2ba123969f6528831a1c` | Merge cgroupns config with existing Docker daemon.json | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | tracker match | no | none | intentionally skipped | This commit only refines the temporary daemon.json rewrite introduced for the cgroupns-host workaround, and upstream later removed that workflow mutation when it switched away from the systemd-mode deb E2E path. |
| `601c7df394830b9b429da51ab84efd0617ed496b` | Add debug output to entrypoint to diagnose e2e test failures | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | direct local match | no | none | intentionally skipped | Upstream change is temporary diagnostic logging in `src/docker/stage/deb/entrypoint.sh` with no durable runtime fix; current local entrypoint likewise omits that debug-only output. |
| `e7aece98b07cf8dc6e8938ac9c401deaada9fead` | Disable systemd mode for E2E tests - run seedsync directly | Subjects 2, 4, and 5 | likely intentional skip | high | direct local match | no | none | intentionally skipped | Current local deb E2E path still intentionally exercises the packaged systemd service through the stage/deb container, so the upstream “run seedsync directly instead of systemd” architecture change was not taken as a conservative default. |
| `fde8158dc5a738f57f48d3688cee6f51530fddb6` | Remove systemctl enable from install script (not using systemd) | Subject 2 - Tests, CI, And Verification Assets | covered elsewhere likely | high | direct local match | no | full | covered elsewhere | Current `src/docker/stage/deb/install_seedsync.sh` already omits any `systemctl enable` command. |
| `d1694dd1e5c10156ffde88b3667c80c420c1b335` | Fix entrypoint to run seedsync as user 'user' with correct HOME | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | direct local match | no | none | intentionally skipped | This change only applies to the upstream direct-run deb E2E architecture; current local stage/deb path still boots systemd and does not use this non-systemd entrypoint flow. |
| `c1a1a5ed177f26e7f6bcb2095d6f6700a4452b65` | Add comprehensive debugging output to E2E test entrypoint | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | direct local match | no | none | intentionally skipped | Upstream adds only transient debug output in `src/docker/stage/deb/entrypoint.sh` and `Makefile`; current local state likewise does not keep those diagnostics. |
| `a782f7e75826573c47be1bfc0b76e5357fa23e6e` | Fix myapp container log capture to use OS-specific container names | Subject 2 - Tests, CI, And Verification Assets | likely intentional skip | high | direct local match | no | none | intentionally skipped | This is a debugging/log-capture refinement for the upstream E2E harness, and local `Makefile` does not carry that auxiliary myapp-container log collection path. |
| `f2b4889765a4cb53475b3a452577fd5024a8bc4c` | Fix scanfs path in deb build - copy to _internal directory | Subject 4 - Packaging And Install | already integrated likely | high | direct local match | no | full | already integrated | Current `src/docker/build/deb/Dockerfile` already copies `scanfs` into `/build/work/seedsync/_internal/`, matching the upstream PyInstaller bundle layout fix. |
| `0cb32280056fcfdad740dc34d8e505e961dc4740` | Fix Bottle _stop_flag attribute conflict in web_app.py | Subject 11 - Model, Serialization, And Web API | possible gap | medium | direct local match | yes | none | needs subject reopen | Current `src/python/web/web_app.py` still uses the older private-attribute pattern with `self.__stop = False` / `self.__stop = True` and has no `object.__setattr__` workaround, so the Bottle attribute-compatibility fix is not defensibly covered locally. |
| `2ae51736a47e993de71e8660fe7e39b5b2e2c78a` | Update deb E2E test matrix for GLIBC 2.29+ compatibility | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `.github/workflows/master.yml`, `Makefile`, and `doc/DeveloperReadme.md` still keep the older `ubu1604`, `ubu1804`, and `ubu2004` deb E2E matrix with no upstream `ubu2204`-only GLIBC 2.29+ narrowing. |
| `4cbdaa549f20e6bd572a8ddc7da93cb5870c59d9` | Update Makefile and docs for GLIBC 2.29+ requirement | Subjects 4 and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `Makefile` and `doc/DeveloperReadme.md` still document `ubu1604`, `ubu1804`, and `ubu2004` as the deb E2E options and do not carry the upstream GLIBC 2.29+/Ubuntu 20.04+ requirement note. |
| `ef283ccf187c2fd6ede23df98c96747f2e8579f3` | Fix HTML path in deb build - copy to _internal/html | Subject 4 - Packaging And Install | already integrated likely | high | direct local match | no | full | already integrated | Current `src/docker/build/deb/Dockerfile` already copies Angular output into `/build/work/seedsync/_internal/html`, matching the upstream frozen-app HTML path fix. |
| `8fac10e480cbb283a3be3f1e452f8f776a46c69f` | Fix E2E test failures for autoqueue and dashboard tests | Subject 2 - Tests, CI, And Verification Assets | possible gap | high | direct local match | no | none | needs subject reopen | Current `src/docker/test/e2e/parse_seedsync_status.py`, `src/docker/test/e2e/run_tests.sh`, `src/e2e/tests/autoqueue.page.ts`, and `src/e2e/tests/autoqueue.page.spec.ts` still lack the upstream wait-and-cleanup hardening for AutoQueue and dashboard E2E stability. |
| `7897c8ea7c4e85aaf59beaf53fd095604d813f3c` | Catch LftpJobStatusParserError in controller to prevent server crash | Subjects 14 and 18 | already integrated likely | high | direct local match | no | full | already integrated | Current `src/python/controller/controller.py` already catches `LftpJobStatusParserError` alongside `LftpError` in the main controller status path. |
| `9e84b9e54d3284a90fb9864b4612fb1ded3f3914` | Handle LftpJobStatusParserError in kill command | Subjects 14 and 18 | already integrated likely | high | direct local match | no | full | already integrated | Current `src/python/controller/controller.py` already catches `LftpJobStatusParserError` alongside `LftpError` in the kill-command error path. |
| `b4c393ef9f3b78c82e1246b79d0d98ae0070531a` | Fix E2E test: trim whitespace from extracted text content | Subject 2 - Tests, CI, And Verification Assets | possible gap | high | direct local match | no | none | needs subject reopen | Current `src/e2e/tests/dashboard.page.ts` still returns raw `getText()` / `innerHTML` values with no upstream `.trim()` normalization for extracted dashboard text. |
| `3f7af5c79aaff746c595b37097e84cbbd61e84e9` | Fix Docker ARM E2E tests: match remote container architecture | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `Makefile` and `src/docker/test/e2e/compose.yml` still lack the upstream `SEEDSYNC_PLATFORM` wiring and remote-container `platform:` pinning for ARM E2E parity. |
| `2e2a60e030d40ca33c6da4795c2b86d5c604f2b8` | Remove redundant platforms build key from compose.yml | Subject 2 - Tests, CI, And Verification Assets | covered elsewhere likely | high | direct local match | no | full | covered elsewhere | Current `src/docker/test/e2e/compose.yml` already has no `build.platforms` key, matching the upstream cleanup direction. |
| `c73205b19c707ac74da18513a0e56ec0d5fcbebb` | Set DOCKER_DEFAULT_PLATFORM for cross-architecture builds | Subjects 2, 4, and 5 | possible gap | high | direct local match | no | none | needs subject reopen | Current `Makefile` still lacks the upstream `DOCKER_DEFAULT_PLATFORM` export when building cross-architecture E2E containers, so only the pull path is architecture-aware. |
