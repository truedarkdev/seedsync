# Integration Tracker

This document is the live, future-facing ledger for upstream integration work.

Use it to answer two questions quickly:
- are we fully processed through the last reviewed fork tip?
- where do we resume when new upstream commits appear?

This tracker is intentionally fork-first, not subject-first.
The detailed subject-by-subject history is no longer kept here.

## How To Use This Tracker

For each active fork, keep:
- the last fully processed upstream commit
- the fork tip seen at the last full review
- the last review date
- the current caught-up status
- a short summary of what is already integrated locally
- a short note for how to resume when new commits appear

Refresh rule:
- fetch remotes first
- compare the current fork tip to `Last fully processed upstream commit`
- if the tip moved, review only the new upstream commits after that commit
- once the new work is processed, update this tracker to the new fully processed tip

## Entry Template

```md
## <fork>
- Source branch: <fork>/<branch>
- Last fully processed upstream commit: <commit>
- Fork tip at last full review: <commit>
- Last full review date: <YYYY-MM-DD>
- Status: caught up through this tip | refresh needed
- Integrated so far: <short summary>
- Resume when new upstream appears: <short instruction>
- Notes: <optional>
```

## thejuran
- Source branch: `thejuran/master`
- Last fully processed upstream commit: `bcebdf8eaec5d3abf5586aad2278f2c77667cd71`
- Fork tip at last full review: `bcebdf8eaec5d3abf5586aad2278f2c77667cd71`
- Last full review date: `2026-03-29`
- Status: closing lane; final legacy commit sweep remaining before removal
- Integrated so far: conservative imports and local adaptations now cover the worthwhile reviewed work through `bcebdf8eaec5d3abf5586aad2278f2c77667cd71`, including the host-header guard, the requests lock refresh, the AutoQueue-to-Settings merge, and the `remote_scanner` quoting hardening. Broader auth/CSP/token-flow, dashboard restyle, Playwright/QEMU timing, and planning/release docs remained intentionally out of this refresh so the default SeedSync behavior stays conservative and recognizable.
- Resume when new upstream appears: fetch `thejuran/master`, review the remaining legacy commits in strict oldest-to-newest order, then remove this lane once that final sweep is complete.
- Notes:
  - This is now a closing legacy lane. `thejuran-arr` is the ongoing successor source, and `thejuran` should be removed from this tracker after the final old-fork commit sweep is fully processed.
  - This refresh has been folded back into the main tracker; the temporary frozen ledger is retired.
  - Deferred from this refresh: the broader auth/CSP/token bundle, dashboard visual restyles, Playwright/QEMU E2E timing churn, and release/planning bookkeeping.
  - Doc/planning artifacts were intentionally skipped, including the six doc-only commits from `00a0c86214261ad58592eb6978b71a948ae91e47` through `fa96fdf303b99919f10698511f12a3801d9f2cfe` and later phase-opening / completion docs.
  - `5c899615b5ce4ece819e766951efe7be10907892` was adapted locally for `api_token` config support and stronger config API redaction; `a29cae1108bf481181bc363903de4d8c6db765e3` was intentionally skipped as planning-only documentation.
  - Phase 48/49 hardening/runtime work split across local adaptations and deferrals: startup warnings (`cd07bfcf`), path-traversal guards and tests, transient SSH scan recovery (`04ed3ace76b760ef6263f22ec376fc97d55f9279`), plus covered-elsewhere items such as `7bf7fb2826b759bf1feb456a250e6d00cb35651c`, `f63e7cf5935433389acce7cc943859a834147ff4`, and `1eae2c56a9f2556d0ead2cafa8cbe93b7fd5349f`.
  - The CI/dependency cluster was treated as Angular-19-specific policy noise against the legacy Angular 4 baseline, with `cea63a27b502869241d704aabe266e18200d167c` split to a separate legacy dependency-audit task.
  - The Angular migration trio landed as `c5eb482bfbf3d8eab73d31f746ef795272a3505a` `intentionally skipped`, `db497146b623aaa860d641c24b78e18919215242` `needs new integration task`, and `3767adafbc3893ad85643de7b2b7212d8dc7b2e9` `needs subject reopen`; the migration-tail commits `c6318bc50b88ab1ffa978852193d61be321cc42a`, `ab74f8c53d9daf1e765315808bf995a6ea01974a`, `32387fca344f2b3c75eb9ab2e0f2cf4eceb58da1`, and `36c13b3f0acf5b9f0df738cd9964a44f31885771` closed the frozen range, and the `c6318bc50b88ab1ffa978852193d61be321cc42a` follow-up is now integrated locally via the Subject 21 note.

## thejuran-arr
- Source branch: `thejuran-arr/main`
- Last fully processed upstream commit: `unreviewed`
- Fork tip at last full review: `e9d1e2627b7492f5025c6a9e55236dcd5b7d23db`
- Last full review date: `pending`
- Status: refresh needed
- Integrated so far: none yet; this is the renamed `thejuran/seedsyncarr` history and it starts as a separate line, not as a continuation of the old `thejuran/seedsync` checkpoint.
- Resume when new upstream appears: fetch `thejuran-arr`, then review the history strictly oldest-to-newest from the first commit in this lane.
- Notes:
  - The old `thejuran` checkpoint `bcebdf8eaec5d3abf5586aad2278f2c77667cd71` remains the processed base for the legacy fork only and does not appear in this lane's history.
  - Local remote HEAD observed for this lane: `e9d1e2627b7492f5025c6a9e55236dcd5b7d23db`.

### Active subject reopens
- Subject 21 - Angular migration follow-up: `3767adafbc3893ad85643de7b2b7212d8dc7b2e9`

## rapidcopy
- Source branch: `rapidcopy/master`
- Last fully processed upstream commit: `dc9c68c37c43eba7487654dacf7c7b08f64eb12a`
- Fork tip at last full review: `dc9c68c37c43eba7487654dacf7c7b08f64eb12a`
- Last full review date: `2026-04-01`
- Status: caught up through this tip
- Integrated so far: locally useful rapidcopy ideas have already been adapted where they fit this fork, especially around path-pairs, UI workflow polish, packaging/runtime hardening, logs/files improvements, and targeted reliability fixes, while branding, theme-system, and other identity-shifting changes remain intentionally out.
- Resume when new upstream appears: fetch `rapidcopy`, then continue from the next commit after `dc9c68c37c43eba7487654dacf7c7b08f64eb12a` in strict oldest-to-newest order.
- Notes:
  - Completed frozen refresh on `2026-04-01`; the frozen range was `1b96fb80938d398d7fca701771f11c13df5a0bc7` through `dc9c68c37c43eba7487654dacf7c7b08f64eb12a`.
  - Per-commit explorer re-audit finalized the disputed frozen-range dispositions as `cb20dc899ccabce453c2ae4d44e9e0153f7a74ea` `intentionally skipped` because the current workflow shape has moved past that exact `if:` guard pattern, plus `acf1a0c64eb4becb14ffec3104613238bd8cbbd5` and `dc9c68c37c43eba7487654dacf7c7b08f64eb12a` as `needs new integration task` because both are stale failure-cleanup items that should only be revived on fresh repro.
  - The temporary frozen-refresh ledger was removed after all 13 commits in the pass received individual explorer audits, so the unresolved follow-up work now lives only in this tracker.
  - Commit `c766bd96366a1da7ffbf8f65df05a6bd904c3106` was adapted locally by hardening `lftp.queue()` command construction and adding targeted queue test coverage; WSL verification covered that adaptation.
  - Adapted rapidcopy commit `674992ac09857dc6fa8ca9642bd6a50597e2bb29` as a local `.select-all` header-checkbox alignment fix; did not take its Python `WebApp.stop()` hunk because local code already uses a consistent private stop flag implementation.
  - `thejuran` now has a new active frozen refresh ledger beyond its prior processed checkpoint.

## nitrobass24
- Source branch: `nitrobass24/develop`
- Full manifest rows: `956 total reachable commits`
- Baseline rows already in `origin/master`: `87` rows, `1-87`, through common base `ff2a1039935beccbbf7ec76134b41d2e91137742`
- Fork-unique audit rows: `869` rows, `88-956`, `ec38aaf6e6ca0ab2479fcd003d15679007101021` through `38d6ef22d36b6a75c164bc754bac9cd2842e8722`
- Initial audit frozen tip: `38d6ef22d36b6a75c164bc754bac9cd2842e8722`
- Last dispositioned fork-unique row: `956` (`38d6ef22d36b6a75c164bc754bac9cd2842e8722`)
- Status: audit complete / deferred integration tasks open
- Manifest: [doc/integration-notes/nitrobass24-initial-audit.md](/mnt/c/Git/seedsync/doc/integration-notes/nitrobass24-initial-audit.md)
- Resume checkpoint: rows `761`-`956` are dispositioned; no frozen nitrobass24 rows remain pending.
- Row `847` scanner/symlink handling is an intentional local security-policy divergence: `src/python/system/scanner.py` skips directory symlinks before recursion, the existing outside-root and cyclic symlink tests lock that behavior, and any future change would need an explicit product/security decision plus a new containment model rather than bug-fix treatment.
- Rows `943`-`956`: row `943` is intentionally skipped as a website-only dependency bump against the absent local `website/` subtree, and row `944` is its wrapper; row `945` is covered locally by the SSHCP run-command regression test lane, with row `946` covered as its wrapper; row `947` is adapted locally in `caace313` for the `.lftp` temp-file final-move deferral race, and row `949` is covered as its wrapper; rows `948`, `950`, and `952` are adapted locally through the grouped FTPS transfer protocol integration, with rows `951` and `953` covered as wrappers; row `954` is already integrated as local release history; row `955` is adapted locally in `src/angular/src/app/services/files/view-file.service.ts` and `src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts`; row `956` is the final release/sync wrapper.
- Rows `928`-`942`: row `928` requires a maintainer decision before any local Angular 22 framework/bootstrap migration, row `929` is intentionally skipped because the changelog target is absent, row `930` is deferred because it only cleans up row `928` migration shims, row `931` remains coupled to a future Angular 22 migration rather than a standalone lint/tooling bump, and row `932` is the Angular 22 merge wrapper; row `933` is intentionally skipped because local repo has no tracked `MODERNIZATION_PLAN.md`, and row `934` is its wrapper; rows `935`, `936`, and `938` are adapted locally through the grouped FTPS transfer protocol integration, row `937` is covered as formatting-only within that FTPS lane, and rows `941`-`942` are covered as FTPS wrappers; row `939` is covered locally by the SSHCP run-command regression test lane, with row `940` covered as its wrapper.
- Rows `924`-`927`: row `924` is covered elsewhere by earlier local backend work for dead-worker reporting/recovery and failed staging-move retry (`b2eae04a`, `d7d62c69`, `4ddea875`, and `4132bf80`), where the failed-move outcome is already represented as pending-completion + rescan/retry plumbing rather than a persisted `MOVE_FAILED`/`move_failed` state; the visible retry/state surfacing stays deferred with row `921` as the separate UI/product decision, and dead-worker recreation remains a distinct backend/runtime question; row `925` is covered as its merge wrapper; row `926` is intentionally skipped because upstream changelog-only release bookkeeping targets absent local `CHANGELOG.md`; row `927` is covered as the changelog merge wrapper.
- Rows `917`-`923`: row `917` is adapted locally in `src/angular/src/app/services/settings/config.service.ts`, `src/angular/src/app/services/autoqueue/autoqueue.service.ts`, and the focused service specs for the mutating-service contract slice only; auth/config redaction constraints remain separate. Row `918` needs area reopen through `view-file-performance-rows890-894` for the ViewFileCommandService extraction; row `920` is adapted locally in `src/angular/src/app/services/files/view-file.service.ts` and `src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts` so command cancellation still tears down the in-flight inner observable without importing the extraction; row `919` is maintainer-decision-needed because adopting upstream CDK virtual-scroll, fixed-height rows, and traceback popovers would be an explicit visible logs UI/performance change; row `921` is maintainer-decision-needed via existing task `file-component-action-guard-row894` because the local Python/Angular file-state models have no `MOVE_FAILED`/`move_failed` state, no visible file-row branch, no retry-for-move action, and no move-failed counter bucket, so the visible mapping crosses backend model + Angular model/view/action semantics and needs UI/product approval before implementation; row `922` is covered as the frontend follow-up merge wrapper; row `923` is adapted locally in `src/python/lftp/job_status_parser.py` for the pget/filename peek-before-consume parser fix, while row `902` is intentionally skipped because its helper-decomposition refactor has no current local user-facing or correctness gain after row `923`; row `905` is already integrated via local parser typing coverage.
- Rows `915`-`916`: row `915` is intentionally skipped because the repo has no tracked `website/` subtree for the React DOM dependency bump; row `916` is covered as its merge wrapper.
- Rows `906`-`914`: row `906` is adapted locally in `config-remote-python-path-row906`; row `907` is intentionally skipped because local repo lacks the upstream-only `command_pipeline.py` surface; row `908` is covered as the refactor-bucket merge wrapper; row `909` is intentionally skipped because the matching workflow surface is absent; row `910` is covered as its merge wrapper; row `911` is covered elsewhere by the evolved Angular dependency state / noncanonical ignored lockfile surface; row `912` is covered as its merge wrapper; row `913` is intentionally skipped because local Angular testing remains Karma/Jasmine rather than Vitest; row `914` is covered as its merge wrapper.
- Rows `902`-`905`: row `902` is intentionally skipped because row `923` already adapted the local parser behavior fix and the remaining upstream helper-decomposition refactor has no current local user-facing or correctness gain; row `903` is covered elsewhere by the evolved ViewFileService / file-selection split, with any exact capabilities-module review kept in `view-file-performance-rows890-894`; row `904` remains deferred because its command/persist module split depends on upstream command-pipeline surfaces absent locally, while the safe ModelUpdater completion-detection slice is handled with row `680`; row `905` is already integrated by local parser typing coverage.
- Rows `889`-`895`: row `889` is adapted locally in `model-builder-performance-row889`; `ModelBuilder` now uses `deque` BFS frontiers and `ModelFile.iter_children()` in traversal-only paths to avoid child-list copies. Row `890` is partially adapted locally in `src/angular/src/app/services/files/view-file.service.ts` and `src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts` for the single-pass bulk-removal slice, while SSE coalescing and filtered-list memoization remain deferred in `view-file-performance-rows890-894`; row `891` is already integrated by local logs-page buffering/trackBy behavior; row `892` is covered elsewhere because local logs-page tracking uses `LogRecord` object identity, so the duplicate-content trackBy collision follow-up is not a local gap; row `893` is covered as a frontend-bugs/performance merge wrapper; row `894` is partially adapted locally through `file-component-action-guard-row894` for the non-visible stale-action and rebind cleanup, while the config/API-key cleared-state preservation hunk is not locally applicable because this branch does not expose a client-side `web.api_key` config value, and the visible `move_failed` mapping remains decision-gated through row `921` / `file-component-action-guard-row894`; row `895` is covered as the performance bucket merge wrapper.
- Rows `896`-`901`: row `896` is intentionally skipped as a standalone Angular `typescript-eslint` bump because local Angular has no lint target/config or direct lint dependency surface to update; row `897` is covered as its merge wrapper; row `898` is intentionally skipped because the repo has no tracked `website/` subtree; row `899` is covered as its merge wrapper; row `900` is adapted locally in `src/python/poetry.lock` to `webob` `1.8.10`; row `901` is covered as the WebOb merge wrapper.
- Rows `886`-`888`: row `886` is maintainer-decision-needed because the local logs UI lacks the exact upstream history-load banner surface; row `887` is split between the same absent-surface/deferred log-history banner/accessibility hunk, a not-locally-applicable config/API-key redacted-sentinel hunk because local Angular config has no `web.api_key` field or client-side `web.api_key` snapshot, local already-covered malformed model-file SSE payload guarding, and an obsolete upstream stream-dispatch spec hunk that does not match the local `stream-service.registry` surface; row `888` is covered as the frontend-bugs bucket merge wrapper.
- Rows `879`-`885`: row `879` is adapted locally in the Angular config/bootstrap path; rows `880`, `881`, `882`, and `884` are already integrated for the non-log-history pieces, while row `884`'s log-history failure-state/banner hunk stays absent-surface/deferred with row `886`'s maintainer-decision banner surface and row `887`'s matching accessibility hunk; row `883` is covered elsewhere because the no-persistence outcome supersedes the sentinel reuse; row `885` is already integrated because the branch does not add client-side API-key persistence.
- Rows `794`-`798`: row `794` is intentionally skipped because local `README.md` deliberately identifies this repository as a maintained integration fork; row `795` is intentionally skipped because the upstream Discord User-Agent fix targets an outbound Discord/webhook sender surface that is not wired in this checkout, whose local notification code is in-app only and whose backend webhook_secret handling is config/redaction/startup-warning only; row `796` is covered as its merge wrapper; row `797` is intentionally skipped as upstream release/version bookkeeping; row `798` is covered as the release sync wrapper.
- Rows `775`-`779`: row `775` is intentionally skipped because local `README.md` deliberately identifies this repository as a maintained integration fork; rows `776` and `778` are intentionally skipped as ignored Angular lockfile-only dependency bumps, and rows `777`/`779` are their merge wrappers.
- Rows `780`-`785`: row `780` is intentionally skipped because local repo has no tracked `website/` subtree; row `781` is covered elsewhere as its merge wrapper; row `782` is intentionally skipped because the only candidate `src/angular/package-lock.json` is ignored and untracked locally; row `783` is covered elsewhere as its merge wrapper; row `784` is intentionally skipped because local repo has no tracked `website/` subtree; row `785` is covered elsewhere as its merge wrapper.
- Rows `767`-`769`: row `767` is covered elsewhere for the overlapping Python test hardening in `test_active_scanner.py` and `test_web_app_job.py`; the remaining upstream Playwright/PathPairsConfig-style hunk targets absent local surfaces. Row `768` is adapted locally in `version-check.service.spec.ts` while the absent Playwright/path-pairs spec hunk and changelog-only hunk are intentionally skipped. Row `769` is intentionally skipped because the local Dockerfile already uses Debian-slim builder/runtime stages.
- Row `668`: adapted locally in a focused `ModelUpdater.sync_persist_to_all_builders()` test; upstream-only `command_pipeline.py` and `model_registry.py` tests were not imported because those module surfaces do not exist in this HEAD.
- Row `669`: intentionally skipped because upstream only updates absent tracked `CLAUDE.md` wording for C901 enforcement; local Ruff C901 enforcement remains tracked separately in row `639`, and gitignored `AGENTS.md` is not a repository submission surface.
- Row `670`: covered elsewhere because upstream only formats an `_make_persist` helper signature that does not exist in the locally adapted row `668` test; the committed local `TestModelUpdater` file already uses compact formatter-friendly structure.
- Row `671`: adapted locally in `src/python/controller/model_updater.py` and `src/python/tests/unittests/test_controller/test_model_updater.py`; the sync boundary now normalizes current pair-prefixed keys and UUID-style legacy-colon pair keys while preserving default filenames that contain colons; upstream-only stale-doc hunks were not imported because there is no local `CLAUDE.md` surface.
- Row `672`: intentionally skipped because upstream-only `PairContext` visibility cleanup targets absent local `src/python/controller/pair_context.py`, and the C901 note targets absent tracked `CLAUDE.md`; local controller/model-updater scan state uses a different surface.
- Row `673`: covered elsewhere because it is a merge-only row with no tree changes relative to second parent `3953f880158852b580d98a6cd12db2ea91811331`; substantive model-updater branch work is already represented by rows `666`-`672`.
- Row `674`: covered elsewhere because the merge carries first-parent `src/python/controller/model_builder.py` state with no distinct row-specific implementation; the merge blob matches first parent `1044cf272c8cda29f594f582de2bb579b7c4da40`.
- Row `675`: adapted locally in `src/python/controller/model_builder.py` as parameter-name-only cleanup for static helper methods; behavior is unchanged.
- Row `676`: adapted locally in `src/python/controller/model_builder.py` as parameter-name-only cleanup for `ModelBuilder.__build_children`; behavior is unchanged.
- Row `677`: covered elsewhere because it is a merge wrapper with no tree difference from component parent `399956cd0f682b2eb8b8c662ffeb7288f0e06df3`; local model-builder refactor/rename work is already represented through row `676`.
- Row `678`: covered elsewhere because the merge wrapper carries `src/python/ssh/sshcp.py` feature-line state already represented by row `662` / local `e8866bcb562f650f4289fa7893b11516ab8c02c9` and predecessor `ad1e3ab2c0f0d7605e2248a1db821ce261deaefd`.
- Row `679`: covered locally by the SSHCP run-command regression test lane; mapping showed local `src/python/ssh/sshcp.py` already matched the post-row-945 behavior, so the local adaptation backfilled coverage for the full password-prompt timeout and absence of the unsupported `GSSAPIAuthentication=no` option.
- Row `680`: adapted locally for the safe `ModelUpdater` LFTP completion-detection extraction, with command-dispatch decomposition intentionally left out because local `controller.py` still owns monolithic command handling and upstream `command_pipeline.py` does not exist here.
- Row `681`: covered elsewhere because the merge wrapper matches the row `680` controller-decomposition payload; focused implementation remains tracked in task `controller-decomposition-row680`.
- Row `682`: covered elsewhere because the phase-2 `ModelUpdater.update()` decomposition belongs to the same focused controller/model-updater task `controller-decomposition-row680`; local implementation is not yet present and remains task-owned.
- Row `683`: covered elsewhere because its follow-on `ModelUpdater` polish belongs to the same focused controller/model-updater task `controller-decomposition-row680`; local implementation is not yet present and remains task-owned.
- Row `684`: covered elsewhere because the `ModelUpdater.update() -> None` annotation belongs to the same focused controller/model-updater task `controller-decomposition-row680`; local implementation is not yet present and remains task-owned.
- Row `685`: covered elsewhere because it is the merge wrapper for rows `682`-`684` phase-2 `ModelUpdater.update()` decomposition tracked in task `controller-decomposition-row680`.
- Row `686`: intentionally skipped as a standalone direct `typescript-eslint` bump because local Angular has no lint target/config or direct lint dependency surface to update, and ignored lockfile churn is not canonical integration state.
- Row `687`: covered elsewhere because it is the merge wrapper for row `686` Angular `typescript-eslint` dependency work.
- Row `688`: covered elsewhere because the local Angular package group is already on a newer Angular 21 patch baseline than this upstream bump; later Angular 22 migration remains a separate maintainer-decision item.
- Row `689`: covered elsewhere because it is the merge wrapper for row `688` Angular framework package bump.
- Row `690`: intentionally skipped because upstream bumps direct `vitest`, but local Angular no longer carries `vitest` as a direct devDependency; importing would reintroduce absent tooling.
- Row `691`: intentionally skipped because it is the merge-form of row `690`'s direct `vitest` bump, with no independent payload beyond the absent local direct dependency.
- Row `692`: intentionally skipped because upstream only bumps indirect `postcss` in ignored, untracked `src/angular/package-lock.json`, with no tracked `src/angular/package.json` dependency surface to update under the current Angular lockfile policy.
- Row `693`: covered elsewhere because it is the merge wrapper for row `692`.
- Row `694`: maintainer decision needed with rows `608`/`618` because upstream adds path-pair-aware multi-instance Sonarr/Radarr routing with new persisted instance config, API endpoints, settings UI, secrets handling, outbound service calls, and completion side effects; if approved, split backend/security and UI-approved slices rather than importing wholesale.
- Row `695`: covered elsewhere because upstream bundles Python Ruff/Pyright cleanup for divergent path-pair/integration surfaces with Playwright selector-scoping changes against absent local E2E files; route any future local equivalents through the existing Python tooling and Playwright/E2E modernization lanes rather than importing wholesale.
- Row `696`: maintainer decision needed with rows `608`/`618`/`694` because upstream review hardening for multi-instance Sonarr/Radarr depends on absent integrations UI, `arr_target_ids`, migration, and notifier surfaces; if approved, preserve attach/detach error reporting, duplicate-target guarding, migration recovery ordering, and visible badge contrast in the split implementation.
- Row `697`: maintainer decision needed with rows `608`/`618`/`694`/`696` because upstream hardens malformed integration config fallback, integration refresh errors, path-pair `arr_target_ids` cross-validation, and multi-instance notifier logging against feature surfaces absent locally; if approved, include these recovery and validation invariants in the broader Sonarr/Radarr implementation.
- Row `698`: covered elsewhere because upstream only adds `zip(..., strict=True)` and whitespace cleanup in absent row-specific tests, while local Python is already 3.11+ and adjacent path-pair/controller behavior is covered by local tests.
- Row `699`: covered elsewhere because upstream only adds explicit button types to the absent integrations component, while comparable local settings buttons already use `type="button"`.
- Row `700`: covered elsewhere because it is the merge wrapper for the multi-instance Sonarr/Radarr rows `694`/`696`/`697`, with its tree identical to the PR head and no independent merge-resolution payload.
- Row `701`: needs the row `331` notification-system area reopen because upstream adds Discord/Telegram provider presets, sensitive webhook/token/chat config, formatter/notifier dispatch, `/test/*` endpoints, and visible settings UI that must be designed with UI approval, auth, redaction, logging, and outbound-call safety.
- Row `702`: needs the row `331`/`701` notification-system area reopen because upstream hardens provider UI disabled states, `webhook_url` redaction, Telegram Markdown escaping, outbound event context, and notification-test 400 response content types against absent local notification surfaces; the reusable JSON-error content-type pattern can be considered in that lane or a separate local hardening slice.
- Row `703`: needs the row `331`/`701`/`702` notification-system area reopen because upstream escapes Discord/Telegram backticks and adds HTTP(S)-only URL validation for notification test endpoints, both against absent local notification formatter/handler surfaces; treat the URL scheme check as SSRF/misuse hardening inside that lane.
- Row `704`: needs the row `331`/`701`-`703` notification-system area reopen because upstream fixes `_fire_raw` thread bookkeeping in an outbound notifier absent locally; preserve the remove-on-start-failure invariant if that notifier is later designed.
- Row `705`: covered elsewhere because it is the clean merge wrapper for the Discord/Telegram notification branch already deferred to row `331`/`701`-`704`, with no independent merge-resolution payload.
- Row `706`: covered elsewhere because upstream only fixes compact add-button orientation in the absent integrations header, while comparable local settings header/action buttons already override shared vertical button layout with row-oriented patterns.
- Row `707`: covered elsewhere because it is the merge wrapper for row `706`'s integrations-header compact-button styling, with its tree identical to the PR head and no independent payload beyond the local settings button layout pattern already noted.
- Row `708`: intentionally skipped because upstream only aligns the absent integrations header with path-pairs styling; local path-pairs/settings headers already have their own styling, and any future integrations UI import must follow the UI approval workflow.
- Row `709`: intentionally skipped because upstream only fixes dark-mode styling for an absent `arr-picker` dropdown; local dropdown surfaces already have explicit dark styling, and any future Arr picker UI import must follow the UI approval workflow.
- Row `710`: intentionally skipped because upstream only broadens a Playwright `SettingsPage.getSection()` selector in absent `src/e2e-playwright`, while local legacy Protractor E2E has no matching helper.
- Row `711`: already covered locally because the current settings shell already renders the Path Pairs accordion header as a semantic `h3`, and the remaining upstream integrations-header target is absent locally; adding another nested heading would be a separate UI decision rather than required row-711 integration.
- Row `712`: covered elsewhere because it is the clean merge wrapper for row `711`'s semantic settings-heading UI/accessibility payload, with no independent tree delta beyond second parent `c05fc01cbfe44b17960ac34929b0db3921ba25d4`.
- Row `713`: already integrated because local `settings-page.component.scss` and `path-pairs.component.scss` keep dark header backgrounds/borders without the upstream-removed `color: #8A93A6` override.
- Row `714`: covered elsewhere because it is the clean merge wrapper for row `713`'s dark header text-color cleanup, with an empty second-parent diff and no independent payload.
- Row `715`: intentionally skipped because upstream only adds nitrobass24 v0.15-v0.17 release metadata, `MODERNIZATION_PLAN.md` notes, and an Angular package version bump; local release identity differs, those root docs are absent, and substantive Arr/notification/UI themes are already tracked in their deferred lanes.
- Row `716`: covered elsewhere because it is the clean merge wrapper for row `715`'s nitrobass24 release metadata/versioning payload, with no independent second-parent delta.
- Row `717`: intentionally skipped because upstream deletes `src/docker/test/angular` and `src/docker/test/python`, but this repo still intentionally exposes those Makefile/doc-supported Python and Angular validation lanes; rows `849`/`850` already adapted the live SSH gating/docs without changing that topology, so do not import the deletion without a separate modernization task.
- Row `718`: covered elsewhere because it is the clean merge wrapper for row `717`'s Docker test infrastructure deletion, with no independent payload beyond the preserved local test-topology decision.
- Row `719`: intentionally skipped because upstream rewrites absent `src/docker/build/test-image/Dockerfile`; local Makefile test lanes still use `src/docker/test/*` compose files, and local build surfaces are `src/docker/build/docker-image` and `src/docker/build/deb`.
- Row `720`: covered elsewhere because it is the clean merge wrapper for the Docker test-image Alpine rewrite and related test-infrastructure cleanup already represented by rows `717`-`719`, with no independent second-parent payload.
- Row `721`: maintainer decision needed for the broader Docker/CI/Python-support lane because upstream collapses the main Docker image toward Python 3.13 Alpine and updates CI Python versions, while local `src/docker/build/docker-image/Dockerfile` remains Debian-based with Node/npm compatibility assumptions and local Python support is still capped at `>=3.11,<3.13`.
- Row `722`: covered elsewhere because it is the merge wrapper for row `721`'s Python 3.13 Alpine Docker/CI topology change, with no separate independent payload.
- Row `723`: maintainer decision needed with row `721` because its `uv` `RUN --mount` image-size optimization is embedded in the same Alpine/Python 3.13 Docker platform decision lane, while local `src/docker/build/docker-image/Dockerfile` remains Debian/Poetry-based with no matching `uv` build surface.
- Row `724`: adapted locally for config-write persistence coverage, controller-owned LFTP runtime reconfiguration after successful runtime-only tuning writes, backend-owned restart-required settings metadata, and UI-approved settings feedback that shows restart instructions only when needed and a short-lived immediate-applied banner for hot-applied settings; scanner-dependent settings such as `use_temp_file` stay restart/path-pair-refresh scoped.
- Row `725`: deferred via closed task `playwright-e2e-row725` (docs-only disposition in `2ed9170a`) because upstream adds Playwright-only file-action and error-state specs under absent `src/e2e-playwright`; this is not a direct legacy-E2E port, local legacy `src/e2e` only covers basic dashboard/file-action smoke, Angular unit specs cover much of the action logic, and browser-level error-state/no-enabled-pairs/restart-notification coverage remains a deferred Playwright/browser strategy item that overlaps the UI decision lanes.
- Row `726`: maintainer decision needed with row `721` because upstream adds a Python web integration-test job to absent `.github/workflows/ci.yml` using Python 3.13/uv and fixes CI-exposed web integration tests, while local CI uses `.github/workflows/master.yml` plus Makefile/Docker-compose Python test lanes.
- Row `727`: adapted locally in the WebApp/admin-handler auth boundary as part of the grouped web-security batch with row `847` provenance; the shared response wrapper/hook now applies the browser-facing security header bundle (`X-Frame-Options`, `Referrer-Policy`, global `X-Content-Type-Options: nosniff`, plus the existing connect-src-only CSP for outbound connection policy) across HTML, JSON, redirect, and error responses, and the existing bootstrap/remember/admin tests cover the current surface without importing a separate `web.security` module.
- Row `728`: covered elsewhere because the upstream controller core unit-test assertions are already represented by local tests in `test_controller.py`, `test_scan/test_active_scanner.py`, `test_scan/test_local_scanner.py`, and `test_delete_process.py`; the exact upstream module-specific test-file layout is not imported.
- Row `729`: already integrated because local `Context` and `WebAppJob` code and tests already cover the upstream row's child-context, redacted context logging, args serialization, job lifecycle, server stop, and request-logging middleware assertions.
- Row `730`: covered elsewhere because restart, status, and stream-log handler behavior is already represented by local integration/unit coverage; the only non-overlapping `status.no_enabled_pairs` assertion belongs with the existing disabled-pairs status/UI decision lane rather than this test-only row.
- Row `731`: covered elsewhere because local Jasmine/Karma `ViewFileFilterService` coverage already exercises the upstream name, status, AND, path-pair, and redundant-emission behavior; upstream's colocated Vitest spec layout is not imported while local Angular test tooling remains Karma/Jasmine.
- Row `732`: maintainer decision needed with rows `608`/`618`/`694`/`696`/`697` because upstream Playwright CRUD tests target the deferred Sonarr/Radarr integrations feature surface; local repo has no integrations UI/backend implementation or Playwright E2E equivalent to test yet.
- Row `733`: adapted locally in `src/angular/src/app/tests/unittests/services/settings/path-pair.service.spec.ts`; local AutoQueueService tests were already sufficient, and the PathPairService gap is now covered with Jasmine/Karma assertions for update refresh replacement, delete refresh filtering, and 409 error propagation.
- Row `734`: adapted locally in `component-tests-row734` for local FileOptionsComponent status-filter enablement/reactive file-list updates, service delegation, and OptionComponent debounce/distinct emission behavior; upstream IntegrationsComponent CRUD assertions remain with the Sonarr/Radarr maintainer decision lane because that UI surface is absent locally.
- Row `735`: adapted locally in `src/angular/src/app/tests/unittests/pages/main/header.component.spec.ts` and `src/angular/src/app/tests/unittests/services/utils/version-check.service.spec.ts`; local Jasmine/Karma coverage now covers header waiting/server-down recovery plus remote-scan coexistence/duplicate replacement, and VersionCheckService newer/current/older plus `v`-prefixed release-tag handling and release URL text, while `noEnabledPairs` remains with the disabled-pairs status/UI decision lane.
- Row `736`: intentionally skipped because upstream caches Playwright browsers and npm dependencies in absent `.github/workflows/ci.yml` using absent `src/e2e-playwright/package-lock.json`; local CI still uses `master.yml` and Protractor-based `src/e2e`.
- Row `737`: intentionally skipped because upstream only tracks Playwright cache plumbing via absent `src/e2e-playwright/package-lock.json`; local still lacks the Playwright subtree and `.github/workflows/ci.yml` cache surface, and adding that future subtree would also need an explicit package-lock allowlist because package-lock files are broadly ignored.
- Row `738`: maintainer decision needed with row `721` because upstream's README-only badge update claims Python 3.13 and static test counts while local Python support remains `>=3.11,<3.13`, the runtime image is still Python 3.12, and no matching local CI/Docker change has landed; do not import the README claim before runtime/CI support is decided.
- Row `739`: deferred via closed task `settings-e2e-row739` (docs-only disposition in `dad78977`) because upstream is a Playwright-only settings coverage expansion under absent `src/e2e-playwright`, while local legacy `src/e2e` only has a thin settings title smoke. Most deeper settings semantics are already covered by Angular unit tests, Notifications/Integrations assertions are structurally absent or decision-gated, and this should not be ported wholesale into legacy `src/e2e`; defer to future Playwright/browser modernization, or at most a tiny existing-surface smoke if that path is explicitly chosen later.
- Row `740`: adapted locally in `README.md` only, adding the supported API keys/auth and staging directory notes while omitting the unsupported notification and Sonarr/Radarr claims.
- Row `741`: intentionally skipped because upstream only stabilizes absent Playwright settings-spec waits in `src/e2e-playwright/tests/settings.spec.ts`; local repo still uses legacy `src/e2e` and has no matching browser test to update, so keep as provenance for any future Playwright settings lane.
- Row `742`: intentionally skipped because upstream only changes absent `.github/workflows/ci.yml` and `src/e2e-playwright` Playwright CI/cache surfaces; local repo still uses `.github/workflows/master.yml` and legacy `src/e2e`, so there is no local equivalent to import.
- Row `743`: adapted locally in unittest/integration setup paths by registering `addCleanup(logger.removeHandler, handler)` for root/shared `StreamHandler` instances; production logging behavior is unchanged.
- Row `744`: intentionally skipped because upstream is a website-only Docusaurus `@docusaurus/preset-classic` bump under absent `website/` package surfaces, and this repo has no tracked `website/` or equivalent docs-site dependency surface to update.
- Row `745`: intentionally skipped because upstream is a website-only Docusaurus `@docusaurus/faster` dependency bump under absent `website/` package surfaces, and this repo has no tracked `website/` or equivalent docs-site dependency surface to update.
- Row `746`: adapted locally in `src/angular/package.json` by keeping the exact-pinned Angular manifest shape, bumping the tracked framework/compiler packages to `21.2.11`, bumping `@angular-devkit/build-angular` and `@angular/cli` to `21.2.9`, and leaving the ignored `src/angular/package-lock.json` out of versioned state.
- Row `747`: intentionally skipped as a standalone Angular direct-devDependency `typescript-eslint` bump because local `src/angular/package.json` still lacks a direct lint dependency/config surface and the lockfile-only half is ignored.
- Row `748`: already integrated because local tracked `src/angular/package.json` already pins `jsdom` at `^29.1.1`, and the remaining upstream delta is confined to ignored `src/angular/package-lock.json`.
- Row `749`: intentionally skipped because upstream is a website-only Docusaurus `@docusaurus/module-type-aliases` bump under absent local `website/` package surfaces, and this repo has no tracked `website/` or equivalent docs-site dependency surface to update.
- Row `750`: already integrated because local tracked `src/angular/package.json` already pins `eslint` at `^10.3.0`, and the remaining upstream delta is confined to ignored `src/angular/package-lock.json`.
- Row `751`: intentionally skipped because upstream is a website-only Docusaurus `@docusaurus/types` dependency bump against absent local `website/` package surfaces, and this repo has no tracked `website/` or equivalent docs-site surface to update.
- Row `752`: intentionally skipped because upstream is a website-only Docusaurus `@docusaurus/core` patch bump under absent local `website/` package surfaces, and this repo has no tracked `website/` or equivalent docs-site dependency surface to update.
- Row `753`: adapted locally / partly covered - queue-backed `ActiveScanner` and `MultiPathActiveScanner` tests now register cleanup on construction with idempotent scanner close hooks; upstream Playwright/integration test hardening, Angular release/version metadata, and unrelated frontend/unit-test wording or timer cleanup were not imported in this row.
- Rows `754`-`756`: dispositioned as one v0.16 release/follow-up provenance batch. Row `754` is covered elsewhere as a release wrapper over already dispositioned Sonarr/Radarr, mobile scroll, lint, and dependency/version work; row `755` is folded into the existing Sonarr/Radarr maintainer decision rather than imported piecemeal; row `756` is intentionally skipped because upstream's file-backed paginated log cursor exhaustion fix has no direct local match in the live SSE/in-memory log handler.
- Rows `757`-`760`: dispositioned as one v0.15 release-cluster provenance batch. Row `757` is covered elsewhere as a release wrapper over already tracked virtual scrolling, sorting, logging, and bulk-delete work; row `758` is intentionally skipped as changelog wording only; row `759` is covered by local logging/files-page lanes or release-doc wording; row `760` is the merge wrapper with no independent payload.
- Rows `761`-`762`: dispositioned as a provenance mini-batch. Row `761` is a release-branch merge wrapper with no independent payload, and row `762` is covered because local files SCSS already lacks the stale variable override while the upstream `integrations.py` formatting hunk targets the absent Sonarr/Radarr integration surface.
- Integrated so far: earlier adaptation evidence is tracked for chunks `88-127`, `128-167`, `168-207`, `208-247`, `248-287`, and `288-327`, plus rows `347`, `353`, `361`, and `666`; later adapted rows through the current checkpoint are summarized in the resume paragraph (`380`, `387`, `388`, `404`, `415`, `416`, `421`, `444`, `445`, `449`, `450`, `460`, `461`, `462`, `466`, `470`, `471`, `492`, `493`, `494`, `495`, `496`, `498`, `499`, `505`, `507`, `529`, `531`, `532`, `533`, `535`, `546`, `549`, `563`, `571`, `572`, `580`, and `582`, including row `421` in `b885ac9c`). Rows `88`, `94`, `95`, `100`, `102`, `123`, `129`, `137`, `141`, `142`, `143`, `165`, `179`, `183`, `185`, `186`, `193`, `195`, `249`, `250`, `251`, `253`, `256`, `258`, `260`, `267`, `268`, `279`, `295`, `297`, `300`, `301`, `302`, `303`, `304`, `306`, `307`, `309`, `310`, `327`, `347`, `353`, and `361` are locally adapted across `89362f026db73c655d4a286c57c5abbe860c8fcb`, `1d30a9ec7c85a80801f8a04d2e56a6e3db269f95`, `391b171a0797d45745e5908d513181b7e97a8751`, `c5d042018043cc274603e075b166bfa6b519b354`, `5368c551be44634cf4178510ca15cda1700242d5`, `edbdeac2ff7bb034fdd25700beef9523bf75816a`, `ff1499f4b41f9b6abb61d5fc446992aebcdd554e`, `e99ac88cd5bc33a7ac1a1494d47af09d24c36906`, `9f57de252b156587da48913e29809cf3c04724a3`, `0e189f2ccb9ddd3378347a56a47620d02e9001ea`, `e9b50b39638a6fd7327d2480393a986e3cf2ead2`, `b2049ad13e0fb98eca019bb764c1fa31ab725adc`, `e55e6a4402b6b715cab09a1311d14c47186b8988`, `a9d02db2feaf03923207fc394356eef126c3c6e0`, `f7d52a4e537013fd52e8dfaf70f5885a954cddaa`, `1df4fa8601a08dd2a0a960cde19b2ad4dd3efae1`, `aa46ccd3814de04198e41ecdabf1d1d1d6354161`, `ae841fb1df6c88a8d8fd60641abcca5681588b6f`, `9d9c795a`, `cedadd93`, `46cd02fe`, `f1a407c5`, `3e2485e5`, `c1a3a77c`, `518422ee`, `034832ed`, `f3f5c790`, `e50166d2`, `76902780`, and `c3003fda`; chunk `168-207` currently has `15 already integrated`, `16 covered elsewhere`, `3 intentionally skipped`, `0 needs area reopen`, `0 needs new integration task`, `6 adapted rows`, and `0 maintainer decision needed`.
- Chunk `248-287` is dispositioned: `0 already integrated`, `17 covered elsewhere`, `8 intentionally skipped`, `0 needs integration/adapt locally`, rows `273` and `282` are adapted locally in `ci-build-topology-rows273-282-286` via a prebuilt Angular build-context handoff on the Docker-image build path in `.github/workflows/master.yml` and `Makefile`, row `286` is adapted locally in the same slice with the Docker-image Angular prebuild/build-context handoff plus conservative `.dockerignore` and reusable `test-image` path, while broader artifact-sharing and deb/release topology remain deferred in `ci-build-topology-rows273-282-286`, row `285` is adapted locally with the `7zip` package swap intentionally skipped because the Bullseye runtime base has no package candidate, 15 adapted rows (`249`, `250`, `251`, `253`, `256`, `258`, `260`, `267`, `268`, `273`, `275`, `279`, `282`, `285`, `286`), and `0 maintainer decision needed`.
- Chunk `288-327` is dispositioned: `0 already integrated`, `18 covered elsewhere`, `10 intentionally skipped`, `0 needs integration/adapt locally`, `0 needs area reopen`, `0 needs new integration task`, 12 adapted rows (`295`, `297`, `300`, `301`, `302`, `303`, `304`, `306`, `307`, `309`, `310`, `327`), and `0 maintainer decision needed`.
- Queue / status: chunk `288-327` normal implementation queue and security-hardening area reopen lane are empty. Row `331` webhook notifications needs area reopen for a holistic notification system rather than a standalone import of the early minimal webhook patch; future work must handle visible settings UI approval plus webhook URL privacy/redaction. Row `332` historical log query/search needs a new integration task because it adds a persisted log API and visible log search/filter UI, must preserve `/server/*` auth scope enforcement, and needs adaptation to the legacy Angular/log-stream stack. Row `333` CI branch publish support belongs in the existing CI topology area reopen task `ci-build-topology-rows273-282-286`, because upstream targeted a now-absent `feat/v0.13.0` branch and this repo currently uses master/tag-oriented publishing. Rows `363` and `373` also belong in that CI/Docker build-topology reopen lane with rows `333` and `352`: row `363` adds a `publish-develop` job to absent `.github/workflows/ci.yml`, while row `373` unifies develop/tag publishing with parallel arm64 and branch-aware Buildx metadata; both depend on reconciling local `.github/workflows/master.yml`, make-driven Docker release flow, missing `Dockerfile.alpine`, and branch/tag policy. Rows `457` and `459` join this CI/Docker topology lane for Debian/Alpine consolidation and prebuilt Angular artifact reuse because upstream targets absent `ci.yml` and a different Dockerfile/Makefile publishing model. Rows `302`, `306`, and `307` security hardening are adapted locally in `3e2485e5`, with a non-blocking residual that external extractors can still perform absolute-path writes outside staged extraction without a stronger sandbox. Row `327` structured JSON logging is adapted locally in `f3f5c790`. Row `336` Paste server removal is adapted locally in `8b21f9f0`, using a bounded stdlib WSGI adapter with raw-path preservation, stream/normal request capacity split, fail-safe stream classification, and request logging. Row `337` multi-path-pair support is covered by the local evolved path-pair architecture, including path-pair migration, pair-aware file identity, controller/runtime routing, API wiring, and Angular file identity state. Rows `338`, `339`, and `340` path-pair review fixes need area reopen together to reconcile malformed path-pair config semantics, explicit path-pair handler JSON object and field validation, persisted `enabled`/`auto_queue` boolean strictness, remove-pair-not-found signaling under the local `PathPairError` contract, exact migration sentinel checks, manager concurrency/locking intent, and local bounded web-server teardown semantics against the evolved local architecture. Row `341` path-pairs merge is covered by rows `337`-`340`; it adds no independent implementation delta beyond that merge payload and branch reconciliation. Row `348` path-pair settings UI CRUD is covered by the local evolved settings architecture, which already provides path-pair CRUD, reorder, confirmation, backend wiring, and unit coverage; no upstream UI parity import should happen before the open path-pair hardening lane is resolved and any future visible UI change follows the UI approval workflow. Row `349` per-pair LFTP/scanner controller runtime is covered by the local path-pair runtime architecture's pair-aware routing, refresh, scanning, transfer commands, and model-building; local implementation intentionally differs by using shared routed LFTP/scanner machinery rather than upstream's explicit per-pair context objects and persisted key namespacing. Row `350` Path Pairs settings UX polish is intentionally skipped because it is upstream-layout-specific visible microcopy and placement parity; local row `348` coverage already owns the evolved path-pair settings UX. Row `351` path-pair override-state behavior is adapted locally in the settings and auto-queue controller slices: enabled path pairs disable the legacy `Server/Local Directory` controls and the global `Enable AutoQueue` toggle with override notes, while path-pair `auto_queue` now governs queue eligibility when enabled pairs exist. Row `353` startup log cleanup is adapted locally in `76902780`. Rows `342` and `344` Python scanfs primary-scanner/PyInstaller removal and self-contained remote scan script work need area reopen: local JSON scan transport is partly covered, but scanner execution, `scan_fs.py` self-containment, `--scanfs` semantics, PyInstaller dependency/build wiring, Docker/deb packaging, compose defaults, docs, and files-page delete-confirm UX need a reconciled scanner/runtime packaging and UI-alignment slice with Linux/Docker verifier coverage. Row `502` also belongs in that scanner/runtime lane because upstream's Python 3.8+ remote-script compatibility target conflicts with the local packaged scanner path and explicit Python 3.11/3.12 scanfs runtime floor. Row `343` stale Paste reference cleanup is covered by row `336`/`8b21f9f0`; upstream modal localization removal is intentionally not taken because local Angular still uses the `Localization.Mod.DELETE_*` strings in active modal delete confirmation flows. Row `345` direct extractor subprocess replacement needs a focused extraction area reopen because local extraction remains patool-backed; adapt the direct ZIP/TAR/RAR/7Z handlers and dependency removal while preserving the local staging/failure behavior and addressing the known absolute-path external-extractor residual from rows `302`, `306`, and `307`. Row `346` is adapted locally in `common.exclude_patterns.py`, `controller/model_updater.py`, `controller/controller.py`, `lftp/lftp.py`, `src/angular/src/app/services/settings/config.ts`, and focused tests; follow-ups rows `429`, `454`, `455`, and `467` are bundled into the same lane. Row `347` ViewFileService composite-key test coverage is adapted locally in `e50166d2`, using local `file_id`/`path_pair_id` identity to cover duplicate-name mapping, selection, action delegation, and update/remove diffs. Rows `309` and `310` Docker/package dependency trimming are adapted locally in `034832ed`. Row `304` eager settings-service initialization and directly required config/autoqueue/logging hardening are adapted locally in `518422ee`. Row `303` Angular CSP-friendly build output is adapted locally in `c1a3a77c`. Row `300` restricted remote scanfs install fallback behavior is adapted locally in `f1a407c5`. Rows `295`, `297`, and `301` explicit-UMASK/runtime-permissions behavior are adapted locally in `46cd02fe`. Row `267` delete-action and remote-delete cleanup behavior is adapted locally in `cedadd93`; row `268` Docker UMASK environment support is adapted locally in `9d9c795a`. Rows `256` and `258` extraction dispatch failure reporting and staging fallback archive lookup are adapted locally in `ae841fb1df6c88a8d8fd60641abcca5681588b6f`; pair-specific staging fallback coverage remains a separate follow-up if needed. Rows `253` and `260` unrar/RAR5 Docker packaging support are adapted locally in `aa46ccd3814de04198e41ecdabf1d1d1d6354161` without upstream's deb12-specific pin or changelog edit. Row `249` parser and controller lifecycle/zombie-handling behavior is adapted locally across `f7d52a4e537013fd52e8dfaf70f5885a954cddaa` and `22908425df8bd2cf586f39990b56bdc581dd2e2a`. Row `250` incomplete staged-directory guarding is adapted locally in `1df4fa8601a08dd2a0a960cde19b2ad4dd3efae1`. Row `279` SSH password log redaction is adapted locally in `a9d02db2feaf03923207fc394356eef126c3c6e0`; a separate non-blocking security follow-up remains for `general.webhook_secret` config API serialization. Rows `273` and `282` are adapted locally in `3ae226c4`, and row `286` is adapted locally in `ci-build-topology-rows273-282-286` with the Docker-image Angular prebuild/build-context handoff plus conservative `.dockerignore` and reusable `test-image` path, while broader artifact-sharing and deb/release topology remain deferred in `ci-build-topology-rows273-282-286`; row `285` is adapted locally with the `7zip` package swap intentionally skipped because the Bullseye runtime base has no package candidate; row `275` is adapted locally in the README/install docs, and row `287` is intentionally skipped because the local repo has no Unraid install-doc surface; rows `261`, `277`, `292`, and `321` are website-only dependency rows and are intentionally skipped because this workspace lacks the website subtree.
- Resume/refresh: current frozen manifest is dispositioned through row `670`. Rows `328`-`670` have been processed with dispositions recorded in the manifest; adapted rows in this span are `336`, `347`, `353`, `361`, `380`, `387`, `388`, `404`, `415`, `416`, `421`, `444`, `445`, `449`, `450`, `460`, `461`, `462`, `466`, `492`, `493`, `495`, `498`, `505`, `507`, `529`, `531`, `532`, `533`, `535`, `546`, `549`, `563`, `571`, `572`, `575`, `576`, `580`, `582`, `593`, `594`, `597`, `601`, `602`, `606`, `611`, `641`, `642`, `644`, `650`, `654`, and `662`, `664`, `666`, and `668`; row `577` is covered elsewhere as the merge wrapper for rows `575` and `576`, and row `581` is covered by the row `580` deferred-command list plus post-loop requeue behavior. Row `582` is adapted locally for delete-deferral debug logging, row `583` is already integrated because the local row `582` adaptation already added those debug logs in ruff-friendly multiline form, and row `584` needs the staging/final move retry area reopen while its delete-concurrency portion is covered locally, and row `585` is covered elsewhere as the row `584` merge wrapper, and row `586` is intentionally skipped as a lockfile-only indirect Angular dependency bump against ignored `src/angular/package-lock.json`, and row `587` is covered elsewhere as its merge wrapper, and row `588` is intentionally skipped as a lockfile-only Vitest/jsdom bump against the ignored Angular lockfile, and row `589` is covered elsewhere as its merge wrapper, and row `590` is intentionally skipped as a lockfile-only indirect Hono bump against ignored `src/angular/package-lock.json`, and row `591` is covered elsewhere as its merge wrapper, and row `592` is intentionally skipped as an absent Vitest dev-dependency/test-stack bump while local Angular remains Karma/Jasmine, and row `593` is adapted locally for the pytest-timeout floor while `poetry.lock` already resolves `2.4.0`, row `594` is adapted locally for the `tblib` floor while `poetry.lock` already resolves `3.2.2`, and row `595` is intentionally skipped as a website-only React dependency bump against absent local `website/` package surfaces, and row `596` is intentionally skipped as a website-only Docusaurus dependency bump against absent local `website/` package surfaces, and row `597` is adapted locally in this pass for the pexpect floor/lock update plus Poetry metadata normalization, row `598` is intentionally skipped as a website-only Docusaurus preset dependency bump against absent local `website/` package surfaces, row `599` is intentionally skipped as a website-only Docusaurus types dependency bump against absent local `website/` package surfaces, and row `600` is intentionally skipped as a website-only Docusaurus module-type-aliases dependency bump against absent local `website/` package surfaces; row `601` is adapted locally for the tracked Angular manifest by applying the local-equivalent framework/tooling patch bump, upstream-only `@angular/cdk` and `@angular/build` were not added, ignored `src/angular/package-lock.json` stays out of versioned state, row `602` is adapted locally for the pytest floor/lock update: upstream row 602 was a pytest `9.0.2` to `9.0.3` bump, while local Poetry pytest moved from `7.4.4` to `9.0.3`, including `pluggy` `0.13.1` to `1.6.0` and pytest/Pygments lock metadata, and row `603` is covered elsewhere because upstream only bumps `softprops/action-gh-release` in absent `.github/workflows/ci.yml` while local `.github/workflows/master.yml` publishes releases with `gh release`, and row `604` is already integrated because local `src/python/pyproject.toml` already carries the `ruff>=0.15.10` floor with no direct Ruff lock pin to refresh, and row `605` is already integrated because local `testfixtures = "*"` already permits the upstream requirement while the older lock resolution is a separate optional refresh; row `606` is adapted locally / partly already present, and row `607` needs a maintainer decision because the header portion adds visible `noEnabledPairs` notification behavior while the auto-queue refactor touches divergent path-pair-sensitive runtime flow, and row `608` needs a maintainer decision because the Sonarr/Radarr integration adds settings UI, API-key handling, outbound requests, and completion-time side effects, and row `609` is intentionally skipped as a lockfile-only Hono bump against ignored `src/angular/package-lock.json`, and row `610` is intentionally skipped as a website-only `follow-redirects` lockfile bump against the absent local `website/` subtree, and row `611` is adapted locally for the tracked Angular framework/compiler patch bump to `21.2.9` without adding upstream-only `@angular/cdk` or `@angular/build`, and row `612` is intentionally skipped as a website-only `react-dom` dependency bump against the absent local `website/` subtree, and row `613` is intentionally skipped as an absent GitHub Pages `actions/upload-pages-artifact` workflow bump, and row `614` needs a new UI-approved file-list mobile scrolling integration task because local list rendering diverges from upstream CDK virtual scroll, row `624` belongs with that same UI lane for mobile chrome-height measurement because upstream assumes virtual scroll while local uses paginated `*ngFor`, and row `615` needs a new Angular lint/tooling task because upstream adds Angular ESLint dependencies/config plus lint CI wiring that has no local lint lane, row `622` belongs with that Angular lint/tooling lane because it graduates ESLint/template rules and applies quick-win Angular source edits against tooling local does not yet have, and row `625` belongs with the same Angular lint/type-cleanup lane for `no-explicit-any` replacements plus a small `version-check.service.ts` response guard, and row `616` is intentionally skipped as a website-only Docusaurus `@docusaurus/faster` dependency fix against the absent local `website/` subtree, and row `617` is intentionally skipped because its npm audit security fix maps only to absent `website/` package surfaces plus ignored Angular lockfile-only transitive updates, and row `618` remains with the row `608` maintainer decision because its integrations error-response hardening applies to the deferred Sonarr/Radarr feature surface, and row `619` is already integrated by local context-manager use around the affected controller `__model_lock` paths, and row `620` needs a new file-backed historical logs API/browser task because upstream patches an absent `logs.py` reader while local logs are live SSE only, and row `621` is intentionally skipped because upstream-only `CLAUDE.md` code-health guidance has no tracked local repo surface while local workflow guidance lives in gitignored `AGENTS.md`, and row `622` needs the row `615` Angular lint/tooling lane because upstream combines ESLint rule graduation with quick-win Angular source edits while local has no tracked Angular ESLint config or lint script yet, and row `623` is intentionally skipped because upstream only changes absent `.coderabbit.yaml` review-bot options, and row `624` needs the row `614` UI-approved file-list mobile scrolling lane because upstream assumes a CDK virtual-scroll viewport while local remains paginated `*ngFor`, and row `625` needs the row `615`/`622` Angular ESLint/type-cleanup lane because upstream replaces broad `any` usage while local lacks the lint config/script, and row `626` needs a UI-approved Angular accessibility lane because upstream changes visible template semantics, button/focus behavior, decorative image attributes, and a11y lint gates, and row `627` needs a bounded Angular OnPush/change-detection performance lane because upstream migrates the shell/sidebar/header/files/about components while local shell state handling diverges, and row `628` needs a Python Ruff/B904 lint lane because upstream is mechanical exception chaining while local Ruff selects only `E`/`F` and has no active `B904` ignore, and row `629` needs a Python Ruff/UP032 lint lane because upstream is a mechanical `.format()` to f-string sweep while local Ruff has no active `UP032` enforcement or ignore, and row `630` needs the Python Ruff lint/tooling lane because upstream tightens deferred ignores while local Ruff still selects only `E`/`F`, and row `631` is covered elsewhere as the merge wrapper for the row `629`/`630` Ruff phase work, and row `632` is covered elsewhere as the PR merge wrapper for that Ruff phase2 branch with no standalone payload, and row `633` is covered elsewhere as the branch-sync merge wrapper for the Ruff phase3 line because its non-empty delta is conflict resolution/branch alignment rather than standalone payload, and row `634` is covered by the existing row `628` Python Ruff/B904 exception-chaining lane because it mechanically preserves `FileNotFoundError` context with `from e` while local `extract.py` does not yet mirror that explicit chaining, and row `635` is covered elsewhere as the PR merge wrapper for the row `633`/`634` Ruff phase3 branch content with a tree identical to its second parent, and row `636` is covered by the existing Python Ruff lint/tooling lane because it broadly enables SIM/C4/RET/PERF/RUF rule families plus autofixes while local Ruff still selects only `E`/`F`, and row `637` is already integrated because local `scan_fs.py` already uses `.format()` at the reverted sites and intentionally enforces the Python 3.11/3.12 scanfs runtime floor, and row `638` is covered elsewhere as the PR merge wrapper for the row `637` Ruff phase4 branch content with a tree identical to its second parent, and row `639` needs a new Python Ruff C901 complexity-enforcement task because local Ruff has no `C901`/`max-complexity` policy yet and upstream adds config plus module-specific `noqa` annotations across controller, model builder, lftp, SSH, and divergent logs surfaces, row `640` is covered elsewhere as the PR merge wrapper for that C901 change set, row `641` is adapted locally for auto_queue add/remove text responses by preserving body/status while forcing `Content-Type: text/plain` and `X-Content-Type-Options: nosniff`, row `642` is adapted locally for auto_queue GET JSON responses by preserving the sorted JSON payload while forcing `Content-Type: application/json` and `X-Content-Type-Options: nosniff`, row `643` is covered elsewhere as the row `641`/`642` security-hardening merge wrapper, and row `644` is adapted locally for controller persist-key helper extraction while preserving local JSON file-id compatibility and clearing accepted legacy/unit-separator stopped keys after queue/resume, and row `645` is adapted locally in `common.exclude_patterns.py` as the shared helper extraction that now feeds the row `346` lane, and row `646` is covered elsewhere as the row `645` merge wrapper with a tree identical to the second parent, and row `647` is covered elsewhere as the branch-sync merge reconciling row `644` persist-key extraction with row `645` exclude-pattern helper extraction, and row `648` is covered elsewhere as the row `644` persist-key PR merge wrapper with a tree identical to row `647`, and row `649` is covered by the existing local path-pair/controller architecture from rows `337` and `349` because upstream only extracts pair-context/startup helpers without adding runtime behavior, and row `650` is adapted locally for startup validation of `Controller.use_local_path_as_extract_path` plus conditional `Controller.extract_path` when the flag is false, and row `651` is intentionally skipped as a style-only f-string consistency change in the upstream-only `controller/pair_context.py` module, and row `652` is covered elsewhere as the PR merge wrapper for rows `649`-`651` with a tree identical to the second parent, and row `653` needs a model-registry/listener deadlock area reopen because upstream only extracts the registry boundary; row `654` is adapted locally for the reentrant controller model lock and listener re-entry regression test, row `655` is covered elsewhere as the row `654` merge wrapper with a tree identical to the second parent, row `656` is intentionally skipped as structural-only CommandPipeline extraction because local command lifecycle behavior remains inline in `controller.py`, row `657` is intentionally skipped as cleanup inside upstream-only `command_pipeline.py` with no independent local surface, row `658` is intentionally skipped as typing-only cleanup inside that same absent `command_pipeline.py` module, row `659` is intentionally skipped as public-method/import cleanup for the skipped controller/command-pipeline split, row `660` is covered elsewhere because local inline `__move_from_staging()` already guards missing staging/final paths, row `661` is covered elsewhere because command queue and command/move lifecycle behavior remain inline in local `controller.py`, row `662` is adapted locally for shared Sshcp expect-result classification plus shell-not-found helper extraction while preserving SSH failure mappings, row `663` is covered elsewhere because the local row `662` classifier extraction already decodes `sp.before` once and reuses the string in the shared helper, row `664` is adapted locally for the ModelBuilder build_model helper extraction while preserving local model-building behavior, row `665` is covered elsewhere because the row `664` refactor already removed the old persist-authority pass/elif shape while preserving default-state behavior, row `666` is adapted locally for the ModelUpdater extraction while preserving controller model-update behavior, row `667` is already integrated because local command-pipeline persist sync wiring already uses the public `sync_persist_callback` attribute, row `668` is adapted locally for focused `ModelUpdater.sync_persist_to_all_builders()` coverage while upstream-only command-pipeline/model-registry tests do not map to local modules, row `669` is intentionally skipped as an upstream-only `CLAUDE.md` wording update against absent tracked `CLAUDE.md` while the real C901 enforcement work remains tracked in row `639`, row `670` is covered elsewhere because the upstream `_make_persist` formatting target does not exist in the compact locally adapted row `668` test, and config debug redaction is already covered by the local centralized sensitive-field debug logging path. Row `387` is adapted locally only for the SSH read-only install-docs mount in `src/python/docs/install.md`; the remaining row-387 UI/API hunks are covered elsewhere, obsolete, or divergent, and no runtime UI/API change was made. Row `388` is adapted locally only for the scanner guard in `src/python/controller/scan/remote_scanner.py`; the path-pair validation helper portion belongs in the existing rows `338`-`340` path-pair hardening lane. Row `404` is adapted locally for workflow token-scope tightening in `.github/workflows/master.yml`; the upstream tempfile alert is already satisfied locally in `src/python/tests/unittests/test_common/test_config.py` via safe `NamedTemporaryFile` handling and no `mktemp()` usage in that row 404 config-test surface, so no code diff was needed there. Row `405` is covered elsewhere as the PR merge wrapper for row `404`, with no independent payload beyond the already-adapted CodeQL workflow/test hardening theme. Row `406` is already integrated because local `src/python/controller/extract/dispatch.py` already calls `Extract.extract_archive(...)` directly with no separate pre-extraction `verify_archive` step. Row `407` is already integrated because local extraction failures are terminal, recorded as `EXTRACT_FAILED`, and no retry/re-download loop remains in the current controller/extract architecture. Row `408` is already integrated because the merge commit is tree-identical to row `407`, with no independent delta beyond rows `406` and `407`. Row `409` is already integrated because local auto-queue/controller routing is path-pair-aware via `file_id` identity, with duplicate-name path-pair coverage in local auto-queue/controller multi-path tests. Row `410` is already integrated because local `auto_queue` is path-pair/file-id aware and current candidate/retry typing already uses optional path-pair semantics. Row `411` is already integrated as the merge wrapper for the row `409`/`410` auto_queue path-pair fixes, with no independent local delta. Row `412` is intentionally skipped because upstream only bumps `actions/setup-node` in absent `ci.yml`/`docs-pages.yml`; local `master.yml` has no setup-node usage. Row `413` is intentionally skipped because upstream only bumps `docker/build-push-action` in absent `ci.yml`; local `master.yml` has no build-push-action usage. Row `414` is intentionally skipped because upstream only bumps `actions/upload-pages-artifact` in absent `docs-pages.yml`; local `master.yml` has no GitHub Pages artifact usage. Row `415` is adapted locally by updating the local `master.yml` `Publish artifact` step from `actions/upload-artifact@v4` to `actions/upload-artifact@v7`. Row `416` is adapted locally by updating the local `master.yml` `Download deb package artifact` steps from `actions/download-artifact@v4` to `actions/download-artifact@v8`. Row `417` is already integrated because local controller staging completion clearing already discards pending completion for `DELETED`, `EXTRACTED`, and `EXTRACT_FAILED`. Row `418` is covered by the existing local RAR packaging/extraction lane: Docker already installs `p7zip-rar` and `unrar`, the extractor remains patool-backed, and any future direct-`7zz` source-build/stdout-diagnostics migration belongs in the row `345` extraction area reopen lane rather than a standalone import. Row `419` is covered by the same local RAR packaging/extraction lane because local Docker/patool runtime does not use upstream bundled `7zz`, and the upstream prebuilt-7zip image swap belongs with row `345` only if direct-`7zz` extraction is reopened. Row `420` is covered elsewhere as release metadata only: the upstream changelog/version bump adds no independent implementation delta beyond rows `418` and `419` plus existing CI/release lanes. Row `421` is adapted locally in `b885ac9c` in `README.md` and `src/python/docs/faq.md` with hard-link setup guidance; upstream `website/docs/faq.md` has no local surface. Rows `389` and `390` are covered elsewhere because upstream only bumps untracked `src/angular/package-lock.json` state for indirect `hono`, while this workspace's generated lockfile/node_modules already resolve `hono` at `4.12.9` and there is no tracked package surface to change. Row `391` is covered elsewhere as a no-op merge marker: its merge tree is identical to first parent `5aa82579...`, so the large second-parent surface is not an independent file delta in this merge result. Row `392` is covered elsewhere as a broad v0.13.0 release aggregation: the core multipair/path-pair runtime, API, and UI behavior overlaps the local evolved path-pair architecture with divergent endpoint and response contracts; logs/notifier and infra sub-surfaces remain in focused follow-up lanes rather than being imported from this mixed merge. Row `395` is covered elsewhere as the PR merge wrapper for row `394`, with no independent payload beyond the QEMU action bump already queued. Row `397` is covered elsewhere as the PR merge wrapper for row `396`, with no independent payload beyond the metadata-action bump already queued. Row `399` is covered elsewhere as merge-combination context for row `398` plus the earlier QEMU bump; no standalone payload remains beyond the local workflow action-version alignment already tracked. Row `400` is intentionally skipped because local `master.yml` uses inline `docker login` commands and has no `docker/login-action` dependency to bump; switching to action-based GHCR login would be a separate CI/Docker topology decision. Row `401` is covered elsewhere as the PR merge wrapper for row `400`, with no independent payload beyond that skipped login-action bump. Row `403` is covered elsewhere as the PR merge wrapper for row `402`, with no independent payload beyond the checkout v6 bump already queued. Rows `444` and `445` are adapted locally in this batch for LFTP queue-command logging and pending-completion clearing; rows `449` and `450` are adapted locally in this batch for AppProcess plus scanner/extract/validate queue cleanup, controller shutdown/cleanup call sites, and terminate safety; row `446` is covered elsewhere as the row `445` merge wrapper, and row `451` is covered elsewhere as the row `449`/`450` merge wrapper. Rows `454` and `455` are adapted locally in the controller/LFTP queue propagation and shared parser helper, and row `456` is covered elsewhere as that merge wrapper. Rows `457` and `459` need the CI/Docker topology area reopen because upstream targets absent `.github/workflows/ci.yml`, Alpine-only Docker publishing, and prebuilt Angular artifact reuse that must be reconciled with local `master.yml`, Makefile, Dockerfile, and branch/tag policy; row `458` stays with row `442` in the broader xfer_verify/validate config contract lane. Rows `460`, `461`, and `462` are adapted locally in `d48e570c` for Unraid PTY progress-fragment parser tolerance, `COLUMNS=10000` spawn env preservation, and the intentional 10-error parser tolerance cap with strict threshold comparisons; row `463` is covered elsewhere as their merge wrapper. Queued follow-up lanes include webhook notifications (`331`), historical log search (`332`, plus row `393` logs query/UX reconciliation and row `620` file-backed rotated-log pagination/ordering), CI/Docker build topology (`333`, `352`, `363`, `373`, plus row `394` local `master.yml` QEMU action bump, row `396` publish metadata-action strategy, row `398` local `master.yml` four-site setup-buildx-action bump, row `402` local `master.yml` checkout v6 bump, and rows `508`, `524`, `537`, and `538`), dependency tooling/Dependabot config (`383`, `393`, `543`, together with previously skipped row `314` if reopened), path-pair hardening (`338`-`340`, plus row `393` path-pair handler/API reconciliation), scanner/runtime packaging (`342`, `344`, `502`, `553`, `554`, `555`), direct extraction subprocess replacement (`345`, `377`, `378`), exclude-pattern filtering (`346` plus later follow-ups), path-pair override state (`351`), review-fix split (`356`, plus row `393` Angular file/settings UX reconciliation), Angular accessibility/UI semantics (`626`), Angular OnPush/change-detection performance (`627`), Playwright/e2e modernization (`510`-`520`, `539`-`541`, plus row `526` if a future test-tooling lane reintroduces `jsdom`), future Python type-checking/tooling enforcement (rows `497`, `508`, `561`, row `628` B904 exception chaining, row `629` UP032 f-string modernization, row `630` deferred Ruff-ignore cleanup, row `631` merge wrapper for row `629`/`630`, row `632` PR merge wrapper for Ruff phase2, row `639` C901 complexity enforcement, and later Ruff/Pyright cleanup rows), security/path validation (`529`, `531`, `532`, `533`), model-registry/listener deadlock (`653`, row `654` adapted locally), extraction/path-pair completion and staging behavior (`357`, `365`, `366`, `367`, `584`), and disabled-pairs status/UI behavior (`370`, `371`). Rows `375` and `376` are covered elsewhere because local `__move_from_staging` already short-circuits when the staging source is absent and path-pair staging roots are resolved per pair. Rows `377` and `378` need the existing extraction area reopen with row `345`; row `379` is covered elsewhere because it adds no independent extractor delta beyond rows `377`/`378`, while its side deltas overlap row `373` CI topology and row `375` staging restart safety. Row `381` is intentionally skipped because the local runtime has no `WEB_PORT` contract to preserve, and row `382` is covered elsewhere as the merge wrapper for rows `380`/`381` with no independent payload beyond that already-adapted/skipped healthcheck thread. Row `383` needs a dependency-tooling area reopen because local `.github/dependabot.yml` is absent and the earlier base Dependabot config row was intentionally skipped; row `384` is covered elsewhere as the merge wrapper for row `383`, with no independent payload. Rows `385` and `386` are intentionally skipped because upstream root `CHANGELOG.md`/`MODERNIZATION_PLAN.md` surfaces are absent locally and local docs are MkDocs-only under `src/python/docs`.
- Rows 464-503 are now dispositioned: backend immediate queue empty; row `466` is adapted locally in `51acb106`, rows `470`/`471` are adapted locally in `9020f3ec`, and rows `494`/`496`/`499` are adapted locally in `9a48c8b4`; row `467` is adapted locally in `src/python/lftp/lftp.py`, where directory mirror commands now emit `--exclude-glob` and file `pget` commands stay untouched; row `502` is deferred to the scanner/runtime compatibility lane; CI topology rows `468`/`473`/`475`/`497`; UI-gated / area-reopen rows `478`/`479`/`482`/`483`; rows `492`/`493`/`495` are adapted locally in `33754ea2` for the Poetry-preserving Ruff/Pyright tooling baseline; row `498` is adapted locally in `7d3e08f1` for the small-module Python typing sweep; release/dependency/changelog rows are intentionally skipped or covered by wrappers per the manifest.
- Rows 504-572 are now dispositioned: rows `505`/`507` are adapted locally in `8d29914e` for behavior-preserving Python typing/Pyright cleanup across controller, config, LFTP, SSH, extract, auto-queue, and Bottle web surfaces; row `526` is intentionally skipped because local tracked Angular dependencies have no `jsdom`/package-lock surface to bump; row `528` credential redaction is already integrated, while rows `529`/`531`/`532`/`533` are adapted locally in `a10d7d3e`; row `535` is adapted locally in `74c353ad` for nullable Angular `ModelFile.local_size`/`remote_size` typing and focused null-size specs, while upstream tooltip behavior remains out of scope for this branch; rows `553`/`554`/`555` need the scanner/runtime packaging and compatibility area reopen with rows `342`/`344`/`502` because configurable remote Python path, scanfs runtime-floor policy, config/UI/docs surfaces, trim/quote hardening, and Ruff formatting cleanup must land coherently; row `546` is adapted locally in `ad1e3ab2` for SSH remote target hardening and the duplicate-extract wait-loop fix, while the notifier close change is absent/deferred with the webhook lane, cross-pair extract dedupe is deferred until output isolation is path-pair-aware, and host-only log redaction remains a non-blocking security follow-up; row `549` is adapted locally in `91f92140` for centralized startup required-field validation, field-name-only aggregate errors, enabled path-pair fallback, password-auth vs SSH-key handling, and focused controller/Seedsync coverage; rows `536`/`557` release prep are intentionally skipped; row `558` docs-pages deploy-pages bump is intentionally skipped because local workflows have no docs-pages/deploy-pages surface; row `559` Vitest bump is intentionally skipped because the local Angular stack has no Vitest dependency and remains on Karma/Jasmine with local `0.8.6` package identity; row `560` Angular group bump is covered elsewhere because tracked `src/angular/package.json` already has the target Angular group versions, while local `package-lock.json` is ignored and not integration state; row `561` strict-Pyright migration needs the future Python typing/tooling area reopen with rows `497`/`508` because it spans config, stubs, broad annotations, and runtime-touching cleanup; row `562` website audit lockfile fix is intentionally skipped because upstream only changes absent `website/package-lock.json` and this repo has no tracked `website/` subtree; row `563` is adapted locally in `a5ca259f` by mapping the upstream `uv.lock` Pygments 2.20.0 security bump to local `src/python/poetry.lock`; row `564` release metadata is intentionally skipped because local release identity does not track nitrobass24 0.14.x, while its security/Pyright themes are covered by row `563` and deferred row `561`; row `565` is covered elsewhere because it has no standalone file delta and only summarizes the row `563` Pygments bump plus absent-surface website dependency rows `566`/`567`; rows `566`/`567` are intentionally skipped as website-only dependency bumps against an absent local `website/` subtree, row `568` is covered by the row `563` local Poetry-lock Pygments 2.20.0 adaptation, row `569` is covered elsewhere as a merge wrapper with no net file delta against parent2, and row `570` is covered elsewhere as a no-op merge with a tree identical to both parents; row `571` is adapted locally for the `general.log_level` config/API/runtime/UI migration while preserving legacy `general.debug` compatibility and CLI `--debug`, with broader log-format/settings cleanup tracked separately in `logging-settings-cleanup`; rows `506`/`509`/`521`/`523`/`525`/`527`/`530`/`534`/`542`/`545`/`548` and rows `547`/`550`/`551`/`552`/`556`/`560` are covered elsewhere. Playwright/e2e rows `510`-`520` and `539`-`541`, CI/tooling rows `508`/`524`/`537`/`538`, and uv/tooling rows `543`/`544` are deferred to their focused lane.
- Row `573` is adapted locally in the files-page virtualization lane: the local Angular files page keeps natural-height rows and page-level scrolling instead of importing upstream fixed-height CDK virtual scroll, adds persisted per-page sizing with `25`/`50`/`100`/`1000`/`All`, hides page navigation in `All` mode, defaults the filter/options bar to pinned while respecting explicit unpin, and has maintainer-approved Docker/Playwright browser evidence.
- Queue additions: Rows `423`, `431`, `434`, `437`, and `442` stay in the immediate integration queue; row `427` is adapted locally in `src/python/controller/model_builder.py` and `src/python/tests/unittests/test_controller/test_model_builder.py`; UI-gated rows `438` and `439` need the UI approval workflow before final-stage gates; row `424` now needs area reopen for any future full spawn/logger-transport migration, while row `426` is adapted locally with bounded process/queue lifecycle cleanup for refresh/shutdown/command-process teardown; row `429` is adapted locally in the shared exclude-pattern helper and recursive remote-tree pruning; row `440` is a new Angular service-test task because the local service/test topology differs; rows `441` and `443` need area reopen in the CI/Docker build-topology lane because Debian/Alpine consolidation and prebuilt Angular artifact reuse must be reconciled with local Docker/workflow assumptions.
- Rows `444` and `445` are adapted locally in this batch for LFTP queue-command logging and pending-completion clearing; rows `449` and `450` are adapted locally in this batch for AppProcess plus scanner/extract/validate queue cleanup, controller shutdown/cleanup call sites, and terminate safety; row `446` is covered elsewhere as the row `445` merge wrapper, and row `451` is covered elsewhere as the row `449`/`450` merge wrapper.
- Row dispositions: Rows `422`, `425`, `428`, and `430` are already integrated because the upstream release SHA is present in local history/tag context, the evolved file list/model already exposes path pair label/name behavior, the validate process/status/web/UI flow is present, and the SSE heartbeat handler/registration/tests are present. Row `429` is adapted locally in the shared exclude-pattern helper and recursive remote-tree pruning. Rows `432`, `433`, `435`, and `436` are intentionally skipped because the upstream deltas are package-lock-only, release-metadata-only, or merge-only. Row `440` is a new integration task because high-priority Angular service tests do not match the local service/test layout. Row `441` needs area reopen in the CI/Docker build-topology lane because Debian/Alpine consolidation must be reconciled with local Docker/workflow assumptions.
- Rows `442`-`446` are now dispositioned this batch: row `442` remains `needs integration`, row `443` needs area reopen, rows `444` and `445` are adapted locally, and row `446` is covered elsewhere.
- Rows `447`-`453` are now dispositioned this batch: row `447` stays `needs integration` / UI-gated, row `448` is covered elsewhere as the row `447` merge wrapper, rows `449` and `450` are adapted locally, row `451` is covered elsewhere as the row `449`/`450` merge wrapper, row `452` is already integrated, and row `453` is covered elsewhere as the row `452` merge wrapper.
- Rows `454`-`456` are now dispositioned this provenance batch: rows `454` and `455` are adapted locally in the controller/LFTP queue propagation and shared parser helper, and row `456` is covered elsewhere as the row `454`/`455` merge wrapper.
- Rows `457`-`459` are now dispositioned this provenance batch: rows `457` and `459` need the existing CI/Docker topology area reopen, and row `458` stays with row `442` in the broader xfer_verify/validate config contract lane.
- Rows `460`-`463` are now dispositioned: rows `460`, `461`, and `462` are adapted locally in `d48e570c`; row `463` is covered elsewhere as their merge wrapper.
- Notes:
  - This repo is not GitHub-marked as a fork, but it is SeedSync-derived.
  - `remote.nitrobass24.tagOpt=--no-tags`; branch refs anchor this lane, and the pre-existing local `refs/tags/v1.0.0` is intentionally ignored.
  - `38d6ef22d36b6a75c164bc754bac9cd2842e8722` is the frozen tip only, not a processed checkpoint.
  - The common base with local `origin/master` is `ff2a1039935beccbbf7ec76134b41d2e91137742`; rows `1-87` are baseline accounting rows and rows `88-956` are the fork-unique audit workload.
  - Treat this as a selective source because it is modernization-heavy.
  - Chunk `88-127` is dispositioned: rows `88`, `94`, `95`, `100`, `102`, and `123` are locally adapted, 0 `needs integration` rows remain, row `92` is maintainer-decision-needed in `docker-only-deployment-rows92-93`, row `93` is covered elsewhere via later local CI/docs work, row `122` is maintainer-decision-needed in `angular17-migration-row122` because the literal Angular 4-to-17 standalone import is stale against the current Angular 21 module-based app, 3 `maintainer decision needed` rows (`92`, `105`, `122`), 20 `intentionally skipped`, and 11 `covered elsewhere`.
  - Chunk `128-167` is complete: 4 `already integrated`, 20 `covered elsewhere`, 10 `intentionally skipped`, 0 `needs integration` rows, 6 adapted rows (`129`, `137`, `141`, `142`, `143`, `165`), 0 `needs area reopen`, 0 `needs new integration task`, and 0 `maintainer decision needed`.
  - Chunk `168-207` is complete: 15 `already integrated`, 16 `covered elsewhere` (`172` and `187` folded into the future theming/visualization track), 3 `intentionally skipped` (including row `184` Settings layout), 0 `needs area reopen`, 0 `needs new integration task`, 6 adapted rows (`179`, `183`, `185`, `186`, `193`, `195`), and 0 `maintainer decision needed`.
  - Chunk `208-247` is complete: 27 `already integrated`, 13 `covered elsewhere`, 0 `intentionally skipped`, 0 `needs area reopen`, 0 `needs new integration task`, 0 adapted rows, and 0 `maintainer decision needed`; no local adaptation commit is needed for this chunk because it is already represented in local history.
  - Follow-up: row `249` is adapted locally across `f7d52a4e537013fd52e8dfaf70f5885a954cddaa` and `22908425df8bd2cf586f39990b56bdc581dd2e2a`, and row `251` is adapted locally in `f7d52a4e537013fd52e8dfaf70f5885a954cddaa`; reviewer/verifier accepted rangeless chunk headers, missing chunk data lines, preservation of following non-chunk status lines, focused parser coverage, direct parser probe, Docker/Playwright smoke, post-startup controller AppError propagation, preservation of degraded-startup behavior, full `test_seedsync.py`, HTTP 200, and Playwright bootstrap rendering.
  - Follow-up: row `250` incomplete staged-directory guarding is adapted locally in `1df4fa8601a08dd2a0a960cde19b2ad4dd3efae1`; reviewer/verifier accepted the known-remote-child guard for both recent-snapshot and scan-only staging completion paths, preservation of complete-child and unknown-size-child behavior, focused model-builder coverage, Docker build/start, HTTP 200, and Playwright smoke.
  - Follow-up: rows `253` and `260` RAR5/unrar packaging support are adapted locally in `aa46ccd3814de04198e41ecdabf1d1d1d6354161`; reviewer/verifier accepted full `unrar` availability in the runtime Docker image and Python test image, omission of upstream's deb12-specific pin on the local bullseye base, Docker app rebuild, HTTP 200, and Playwright bootstrap rendering. Actual RAR5 archive extraction through the app/patool path remains an unexercised non-blocking validation gap, and Debian package dependency policy remains a separate follow-up if needed.
  - Follow-up: rows `256` and `258` extraction failure reporting and staging fallback archive lookup are adapted locally in `ae841fb1df6c88a8d8fd60641abcca5681588b6f`; reviewer/verifier/commit-reviewer accepted controller-visible `extract_failed` breadcrumbs, identity-aware active extraction filtering that preserves the active scanner tuple contract, fallback archive lookup through the staging root, focused controller/dispatch/process tests, Docker rebuild, HTTP 200, and Playwright bootstrap rendering. Pair-specific staging fallback remains a separate follow-up if needed.
  - Follow-up: row `279` SSH password log redaction is adapted locally in `a9d02db2feaf03923207fc394356eef126c3c6e0`; reviewer/security/verifier accepted the focused context-log redaction, targeted unit coverage, direct runtime redaction probe, and Docker/Playwright smoke. Security noted a separate non-blocking gap where `general.webhook_secret` may still need config API serialization redaction.
- Follow-up: row `947` staging temp-artifact deferral is adapted locally in `caace313`; reviewer/verifier/security/commit-reviewer accepted the exact-source `src + .lftp` invariant, legitimate `.lftp` payload-name coverage, same-path precedence, symlink/containment hardening, focused controller tests, Docker HTTP 200, and Playwright bootstrap screenshot evidence.
- Rows `799`-`806`: row `799` is intentionally skipped because upstream Vitest bump targets an absent local direct Vitest devDependency/test-stack surface, so do not add it standalone; row `800` is covered elsewhere as the merge wrapper for row `799`; row `801` is intentionally skipped as a standalone `angular-eslint` bump because local Angular has no lint target/config surface to update; row `802` is covered elsewhere as the merge wrapper for row `801`; row `803` is intentionally skipped because tracked `CLAUDE.md` is absent locally and local workflow guidance lives in ignored `AGENTS.md`; row `804` is covered elsewhere as the merge wrapper for row `803`; row `805` is intentionally skipped as a standalone `eslint` bump because local Angular has no lint target/config surface to update; row `806` is covered elsewhere as the merge wrapper for row `805`.
- Rows `807`-`812`: row `807` is intentionally skipped as a standalone `typescript-eslint` bump because local Angular has no lint target/config or direct lint dependency surface to update; row `808` is covered elsewhere as the merge wrapper for row `807`; row `809` is covered elsewhere because the local Angular package group is already on a newer Angular 21 patch baseline than this upstream bump; row `810` is covered elsewhere as the merge wrapper for row `809`; row `811` is intentionally skipped because it is a website-only PostCSS lockfile bump and the local repo has no tracked `website/` subtree; row `812` is covered elsewhere as the merge wrapper for row `811`.
- Rows `813`-`814`: row `813` is maintainer decision needed because upstream introduces `notify_on_download_start` across backend config, notification plumbing, Angular settings UI, and tests, but local config/UI lacks the option and local runtime has no first-class download-start notification boundary; do not import blindly. If accepted, split backend/runtime event semantics first, then config/settings UI/tests. Row `814` is covered elsewhere as the merge wrapper for row `813`.
- Rows `815`-`818`: row `815` adapted locally for persistence-failure rollback and delayed breadcrumb sync callback until after successful persist; row `816` adapted locally for the skip-rollback-if-newer-value guard; row `817` adapted/covered as formatting-only around the touched config handler tests; row `818` adapted locally for per-handler write serialization, with rollback now unconditional inside the handler lock.
- Status update: row `818` is adapted locally and row `819` is covered elsewhere as the merge wrapper for row `818`.
- Rows `820`-`824`: rows `820` and `821` need maintainer decision with the existing deferred Sonarr/Radarr integrations lane because they harden an absent integrations attach/detach handler and related persistence-failure test surface; row `822` is covered as their merge wrapper; row `823` is intentionally skipped as a website-only `webpack-dev-server` dependency bump against the absent local `website/` subtree; row `824` is covered as the row `823` merge wrapper.
- Rows `825`-`827`: row `825` is adapted locally for auto-queue persistence failure handling in `Seedsync.persist()`; path-pairs already return controlled 500s for PersistError but still need rollback/copy-on-write safety. Row `826` needs an area reopen for that path-pair integrity work, while its auto-queue test intent is covered by the row `825` local test. Row `827` is covered as the merge wrapper for that adapted/deferred split.
- Rows `828`-`844`: row `836` is intentionally skipped because upstream only bumps indirect `qs` in ignored, untracked `src/angular/package-lock.json`, with no tracked Angular manifest surface to update under the current lockfile policy.
- Rows `854`-`859`: rows `854`, `855`, `856`, `858`, and `859` are already integrated locally in `src/python/web/handler/config.py`, `src/python/seedsync.py`, `src/python/lftp/job_status_parser.py`, `src/python/controller/auto_queue.py`, and `src/python/web/handler/auto_queue.py` with tests in the matching config, seedsync, job-status-parser, and auto-queue test files; row `857` is adapted locally in `src/python/controller/validate/validate_process.py` with coverage in `src/python/tests/unittests/test_controller/test_validate/test_validate_process.py`.
- Next nitrobass24 work: rows `815`-`827` are dispositioned; rows `828`-`844` are dispositioned; rows `845`-`853` are dispositioned, with row `845` now adapted locally in `src/docker/build/docker-image/Dockerfile`, `src/docker/build/docker-image/entrypoint.sh`, and `compose.local.yml` as the helper cleanup moves into tracked bootstrap code; row `848` is folded into the existing Sonarr/Radarr maintainer-decision lane rather than tracked as a standalone implementation task; rows `854`-`870` are dispositioned; row `860` is adapted locally by mapping upstream move-process failed-result/retry semantics onto the local inline staging move path: failed final moves, including `OSError`, missing-source-without-destination, and missing-root cases, now return failure to `ModelUpdater`, keep completion pending, clear downloaded-marker aliases through the controller persist-key semantics, and force a local scan for retry without importing upstream `MoveProcess`/`CommandPipeline` architecture. Rows `871` and `872` are adapted locally in `src/python/controller/auto_queue.py`, `src/python/web/handler/auto_queue.py`, and the matching controller/integration tests; rows `873` and `875` are adapted locally in `src/python/controller/model_updater.py`, `src/python/controller/controller.py`, `src/python/controller/scan/scanner_process.py`, `src/python/controller/scan/multi_path_scanner.py`, and the matching controller and scanner tests, where move completion now waits for `Controller.__move_from_staging()` to succeed before publishing downloaded/extracted bookkeeping and the post-move retry refresh is narrowed to the owning path pair; rows `874`, `876`, and `877` are adapted locally in `src/python/controller/controller.py` and `src/python/tests/unittests/test_controller/test_controller.py`; row `878` is the merge wrapper and is covered elsewhere. Rows `332` and `351` are paused decision lanes tracked by `historical-log-query-row332` and `path-pair-directory-fields-row351`; row `346` is now adapted locally in `common.exclude_patterns.py`, `controller/model_updater.py`, `controller/controller.py`, `lftp/lftp.py`, `src/angular/src/app/services/settings/config.ts`, and focused tests, with follow-ups `429`, `454`, `455`, and `467` bundled into the same local lane. The Angular Docker build now installs from the tracked manifest only, so the ignored local `src/angular/package-lock.json` mismatch no longer blocks rebuilds; follow-up remains to align the deb packaging lane and developer setup docs with that tracked-manifest policy or reintroduce a reviewed Angular lock/pinning source if reproducibility becomes in-scope.
  - Follow-up: row `183` is adapted locally in `e55e6a4402b6b715cab09a1311d14c47186b8988` as a first-class `Smart Status` sort option, with `Status` preserved as the legacy sort and `Status Reverse` preserved as the reverse mode. Reviewer and verifier accepted the coherent status-header cycle, Smart-vs-legacy ordering, stable Sort button/menu widths, and focused Angular unit coverage.
  - Follow-up: rows `185`, `186`, `193`, and `195` transfer-state stability are adapted locally in `b2049ad13e0fb98eca019bb764c1fa31ab725adc`; reviewer/verifier accepted pending-completion proof before marker persistence, stopped/partial transfer guards, duplicate completion side-effect prevention, active/local model-builder separation, active-overlay cache invalidation, focused regression tests, and rebuilt Docker/browser smoke.
  - Follow-up: row `179` LFTP socket-buffer support is adapted locally in `e9b50b39638a6fd7327d2480393a986e3cf2ead2`; reviewer/security/verifier accepted bounded byte-size validation, blank-to-disable settings behavior, sink-side LFTP setter validation, Angular settings normalization, and live Docker/browser set-and-clear coverage with runtime restored to `8M`.
  - Follow-up: row `129` scanfs architecture is adapted locally in `0e189f2ccb9ddd3378347a56a47620d02e9001ea`; reviewer/security/verifier accepted the default `linux/amd64` remote scanfs platform with explicit `SCANFS_PLATFORM` override, Makefile allowlist guard, Docker image/deb packaging coverage, and residual non-blocking floating-base-image reproducibility risk.
  - Follow-up: row `142` optional-password/config-sentinel subfeature is adapted locally in `9f57de252b156587da48913e29809cf3c04724a3`; reviewer accepted the softened helper copy, verifier confirmed backend/Angular/live Docker/browser behavior, and security accepted the non-blocking existing follow-up that config-set responses still echo raw submitted values.
  - Follow-up: row `143` auto-delete-after-download is adapted locally in `e99ac88cd5bc33a7ac1a1494d47af09d24c36906` as a default-off opt-in setting; security/reviewer gates required and accepted transition-only delete scheduling, no startup or pattern-backfill remote deletion, no reconciliation-only extracted deletion, and reuse of the existing `DELETE_REMOTE` command path.
  - Follow-up: row `141` and the row `142` shell-detection/scanner-install subfeature are adapted locally in `ff1499f4b41f9b6abb61d5fc446992aebcdd554e`; security review accepted residual low non-blocking risk around remediation-text disclosure/copy-paste guidance and commit-readiness recorded the non-blocking diagnostic-breadth caveat for exact login-shell-shaped generic SSH failures.
  - Follow-up: row `137` and the row `142` remote-path/tilde-delete subfeature are adapted locally in `edbdeac2ff7bb034fdd25700beef9523bf75816a`; security review accepted residual non-blocking risks around `rm -rf` being high-consequence and `~user` expansion targeting another remote home when configured.
  - Follow-up: row `165` access logging is adapted locally in `5368c551be44634cf4178510ca15cda1700242d5`; security review accepted the non-blocking tradeoff that normal-mode request forensics are reduced while default request metadata retention improves.
  - Follow-up: row `88`/`100` PUID/PGID support intentionally treats PUID/PGID and mounted paths as trusted operator-controlled Docker inputs; security review accepted residual non-blocking risks around existing principal reuse and reused-account home mutation.
  - Follow-up: row `102`/`123` compatibility is implemented for the Docker image scanfs build only; evaluate the separate `src/docker/build/deb/Dockerfile` scanfs packaging lane when deb packaging compatibility is in scope.

- Row `285` is adapted locally in `src/docker/build/docker-image/Dockerfile`, `src/docker/build/docker-image/entrypoint.sh`, `src/python/tests/unittests/test_web/test_serialize/test_serialize_model.py`, `src/python/tests/unittests/test_web/test_serialize/test_serialize_status.py`, `src/python/pyproject.toml`, and `src/python/poetry.lock`: the runtime image now drops `gosu` in favor of `setpriv`, purges the optional `p7zip-rar` plugin, uninstalls `pip`/`setuptools` after dependency installation, and removes the test-only `pytz` dependency; the upstream `7zip` package swap stays intentionally skipped because the Bullseye base image has no package candidate.
## Reference Notes
- Subject 21 user-facing conflict review: [doc/integration-notes/S21-user-facing-conflicts.md](/mnt/c/Git/seedsync/doc/integration-notes/S21-user-facing-conflicts.md)
  - The confirm-modal replacement and hardening follow-up from `c6318bc50b88ab1ffa978852193d61be321cc42a` is integrated locally.
