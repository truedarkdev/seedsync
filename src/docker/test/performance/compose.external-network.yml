services:
  remote:
    networks:
      default:
        aliases:
          - ${PERF_REMOTE_ADDRESS:?PERF_REMOTE_ADDRESS must name the external-network remote alias}

networks:
  default:
    external: true
    name: ${PERF_EXTERNAL_NETWORK:?PERF_EXTERNAL_NETWORK must name an existing Docker network}
