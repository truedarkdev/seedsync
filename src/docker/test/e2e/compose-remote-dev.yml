version: "3.4"
services:
  remote:
    ports:
      - "127.0.0.1:1234:1234"
    volumes:
      - "${SEEDSYNC_REMOTE_FILES_DIR:?SEEDSYNC_REMOTE_FILES_DIR is required for direct compose runs; set it to a host directory mounted as /home/remoteuser/files}:/home/remoteuser/files"
