version: "3.4"
services:
  remote:
    ports:
      - "127.0.0.1:1234:1234"
    volumes:
      - "${SEEDSYNC_REMOTE_FILES_DIR:?set SEEDSYNC_REMOTE_FILES_DIR before starting the remote test server}:/home/remoteuser/files"
